name: Disabled temporary J-200 transport

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary J-200 synchronization transport disabled; continue only through explicit repository-directed execution."
