name: Disabled one-use dispatcher after I-180 recovery

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Recovery guard
        run: echo 'Automatic downstream dispatch is disabled. Repository restored through the verified I-180 closeout and governance-only I-to-HI transition; follow root README and explicit authorization before any next-module work.'
