name: Disabled temporary J transport after recovery

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary J transport disabled after restoring clean repository-created J-200 startup. Follow ACTIVE_WORK_PACKET only; no fabricated Wolfram captures or prebinding execution."
