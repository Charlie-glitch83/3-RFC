# Incoming uploads

Upload the requested ZIP files into this folder without renaming, unpacking, or modifying them.

After upload, the work model will verify each file, move or register it in the repository location required by the active work packet, and remove this temporary intake folder when it is no longer needed.
