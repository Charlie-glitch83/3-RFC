# Run Plan — D-130

PRE-CHEWED MODULE PLAN

Objective: Evolve the exact frozen C microscopic state through the source-owned conservative nonequilibrium transport law and physically materialize Module D's thermal/distribution, entropy/memory, phase-event, covariance, restart, and handoff state.

The scientific definition is frozen before primary execution in `FROZEN_DERIVATION_SPEC.json` and `PRE_EXECUTION_LOCK.json`. The primary and independent implementations are separately frozen in `IMPLEMENTATION_LOCK.json`.

Exact execution order:

1. Verify the frozen parent, derivation, pre-execution snapshot, provenance bindings, and implementation hashes.
2. Display and register the exact repository-owned Wolfram calls D-WL-001 and D-WL-002 using the connected Wolfram evaluator outputs.
3. Run the exact D manufactured reference check; treat it as implementation evidence only.
4. Execute the packet's exact transport `solver-copy` command and preserve its expected idempotency refusal if the governed startup already copied the exact template/sheet.
5. Materialize the provenance-bound D transport config and run the prebuilt BDF transport solver.
6. Materialize the actual parent-driven RFC physical histories: three-carrier distribution, intrinsic spectral-temperature diagnostic, QV-to-RFL constitutive-energy transfer, entropy production, nonmetric state-space expansion, phase/event chronology, transport/collision operators, and inherited decimal-envelope covariance.
7. Execute semantic countermodels/ablations, dyadic convergence, midpoint restart, and the separately frozen analytic-plus-DOP853 independent reconstruction.
8. Commit the physically executed state without closing D.
9. On a second clean checkout, replay the frozen implementation and compare the generated scientific artifacts.
10. Only after clean replay passes: finalize `H_D_to_E`, manifests, environment, closeout, run registration, and D evidence-state promotions through FROZEN. Do not activate Module E in this state transition.

No historical D result, public thermal history, Kelvin/MeV target, laboratory anchor, Standard Model species identity, or observational transition target may enter generation.
