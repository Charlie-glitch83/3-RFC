# Module G — Wolfram and Independent Verification

## Scope

This file records representative exact and numerical checks for:

- `science/NONEQUILIBRIUM_RECOMBINATION.md`
- `proofs/NONEQUILIBRIUM_RECOMBINATION.md`

These checks verify finite algebraic and implementation consequences only. They do not establish measured atomic precision, public recombination-code agreement, an observed CMB surface, or empirical truth.

## 1. Conservative atomic population generator

For the representative column-conservative generator

```wl
Q = {
 {-3, 1, 0, 1},
 { 2,-3, 1, 0},
 { 1, 1,-2, 0},
 { 0, 1, 1,-1}
};
```

Wolfram returned

```text
column sums: {0,0,0,0}
nonstationary eigenvalues: negative real part
normalization eigenvalue: 0
```

A separate numerical matrix-exponential check retained nonnegative populations and unit total probability.

## 2. Detailed-balance construction

For a positive target distribution `pi` and symmetric conductance matrix `C`, the representative rate matrix was generated by

```wl
Qdb[[i,j]] = C[[i,j]]/pi[[j]]   for i != j
```

with each diagonal set to minus the outgoing column sum.

Wolfram and the independent implementation verified

```text
Qdb . pi = 0
Qdb[[i,j]] pi[[j]] = Qdb[[j,i]] pi[[i]]
```

for every pair in the tested system.

This supports the exact local detailed-balance and stationarity identities used in Module G. It does not supply a public equilibrium history.

## 3. Nuclear-identity and charge pairing

For the toy atomic registry

```text
H+, H0, He++, He+, He0
```

and transition columns

```text
H+ -> H0
He++ -> He+
He+ -> He0
```

the independent check returned

```text
hydrogen nuclear ledger residual = 0
helium nuclear ledger residual = 0
ionic charge change per capture = -1
```

The missing positive charge is paired with one removed free electron, so the full atomic-plus-electron charge ledger closes.

## 4. Transition-energy closure

For a representative downward transition with

```text
E_upper = 3.5
E_lower = 1.0
omega = E_upper - E_lower
```

the independent calculation verified

```text
E_lower - E_upper + omega = 0.
```

This checks the event-level atomic/photon energy pairing used by the transition graph.

## 5. Exact finite line escape

For a two-cell transient line system

```wl
T = {{-(a+b), c}, {a, -(c+d)}};
B = {{b,d}};
Pabs = B . Inverse[-T];
```

Wolfram and the independent implementation returned

```text
Pabs = {{1,1}}
```

for positive representative rates, meaning eventual absorption into the single represented escape outcome has unit probability from either transient state.

This verifies the finite absorbing-chain identity

```text
P_abs = B (-T)^-1
1^T P_abs = 1^T.
```

It is not a measured line-escape calculation.

## 6. Optical depth and visibility normalization

For a constant positive hazard `k` over `[0,T]`, Wolfram evaluated

```wl
tau[t_] := k (T-t)
g[t_] := k Exp[-tau[t]]
Integrate[g[t], {t,0,T}]
```

and returned

```text
1 - Exp[-k T].
```

Therefore the conditional visibility

```text
ghat(t) = g(t)/(1-Exp[-k T])
```

integrates exactly to one.

The independent implementation also verified:

```text
tau'(t) = -k
g(t) = -tau'(t) Exp[-tau(t)]
integral ghat(t) dt = 1.
```

This supports Module G’s explicit retention of the boundary mass `Exp[-tau(eta_in)]` rather than silently assuming infinite initial optical depth.

## 7. Distinct last-scattering and drag distributions

Using two positive hazards over the same finite interval,

```text
k_visibility = 1
k_drag = 2
T = 3
```

the independent implementation returned conditional means

```text
visibility mean = 2.157187089473768
drag mean       = 2.5074547349705334
```

The distributions are therefore distinct even though both have the same normalized hazard form. This checks the Module G typing rule that photon last scattering and baryon-drag release cannot be identified without an equality theorem for their generated operators.

## 8. Positive diffusion Schur form

For the representative stable fast block

```wl
Dfast = DiagonalMatrix[{-2,-5}];
J = {{1},{3}};
Dmat = -Transpose[J] . Inverse[Dfast] . J;
```

Wolfram and the independent implementation returned

```text
Dmat = {{23/10}}
```

which is positive. This supports the stated positive-semidefinite transport form when the fast dissipative block is negative on its physical subspace.

## 9. Covariance propagation

For stable diagonal Jacobian `Jcov`, positive-semidefinite covariance `Sigma`, and positive-semidefinite forcing `Qcov`, the representative Euler step

```text
SigmaNew = Sigma + dt (Jcov Sigma + Sigma Jcov^T + Qcov)
```

retained nonnegative eigenvalues in the independent implementation.

This checks one finite instance of the covariance Lyapunov structure. It does not prove every future discretization stable.

## 10. Independent implementation

A separate NumPy/SciPy implementation returned:

```text
MODULE_G_INDEPENDENT_CHECK: PASS
atomic_markov_positivity_normalization=PASS
restart_semigroup_identity=PASS
detailed_balance_stationarity=PASS
visibility_normalization_reconstruction=PASS
line_escape_absorption_probability=PASS
last_scattering_drag_distinct=PASS
positive_diffusion_schur=PASS
nuclear_identity_and_charge_pairing=PASS
covariance_psd_step=PASS
transition_energy_closure=PASS
```

It used:

- a four-state conservative atomic generator and matrix exponential;
- split-and-restart versus direct evolution;
- a symmetric-conductance detailed-balance construction;
- independent optical-depth differentiation and visibility quadrature;
- a finite absorbing line-transfer chain;
- distinct visibility and drag hazards;
- a stable fast-block Schur transport form;
- a toy H/He nuclear-identity and charge-pairing matrix;
- one covariance Lyapunov step;
- one atomic/photon energy closure identity.

## 11. Wolfram outputs retained

The representative Wolfram calculations also returned:

```text
four-state generator eigenvalues:
-4.324717957244746...
-2.337641021377627... +/- 0.562279512062301 i
0
```

so every nonstationary mode in that example has negative real part.

For the finite visibility example, Wolfram returned:

```text
visibility conditional mean = 2.1571870894737678...
drag conditional mean       = 2.5074547349705338...
```

and for the line-transfer example:

```text
absorption probability matrix = {{1,1}}.
```

## 12. What these checks establish

They support the finite claims that the representative constructions possess:

- conservative positive population evolution;
- restart identity on autonomous subintervals;
- equilibrium stationarity under a detailed-balance construction;
- nuclear-identity and charge pairing;
- event-level transition-energy closure;
- normalized finite line-escape probabilities;
- exact optical-depth/visibility reconstruction;
- conditional visibility normalization with explicit boundary mass;
- distinct last-scattering and drag distributions;
- positive generated diffusion in the tested Schur reduction;
- positive-semidefinite covariance in the tested step.

## 13. What these checks do not establish

They do not establish:

- measured hydrogen or helium rates;
- public multilevel recombination accuracy;
- measured opacity or optical depth;
- an observed visibility curve or last-scattering coordinate;
- public CMB normalization;
- convergence of every full atomic/frequency/angular/spatial realization;
- final angular spectra, matter transfer functions, or empirical agreement.

The exact assumptions, finite support, branches, truncation bounds, source firewall, and claim boundaries in the science and proof files remain controlling.