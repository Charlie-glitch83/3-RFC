# Module G → Module Hᵁ Scientific Handoff

## Purpose

This file seals the scientific boundary between the completed primordial recombination and physical CMB-surface state of Module G and the background-parameterized linear response operator constructed in the first phase of Module H.

It defines only:

- what completed Module G must export;
- which Module G branches are admissible for linear perturbation evolution;
- what Module Hᵁ inherits unchanged;
- what Module Hᵁ must derive before Module I exists;
- what remains forbidden until Module I supplies the realized background;
- what frozen operator state Module Hᵁ must pass to Module I;
- how the later Module H[I] instantiation is protected from redesign or retuning.

It does not import the former run system, run numbering, source-register bureaucracy, lifecycle machinery, gate taxonomy, certificate architecture, or evidence paperwork.

---

# 1. Protected dependency order

The boundary is deliberately split:

```text
Module G
-> Module Hᵁ
-> Module I
-> Module H[I]
-> Module J
```

The scientific meaning is:

```text
Module G:
physical recombination, opacity, visibility,
last-scattering and drag surfaces,
atomic/radiative source histories

Module Hᵁ:
background-parameterized linear operator,
complete perturbation state,
mode basis, gauges, hierarchies,
collision/source grammar,
Green-function and line-of-sight grammar,
admissible background domain

Module I:
realized geometry, expansion, clocks,
distances, horizons, sector backgrounds,
and the realized packet B_I

Module H[I]:
immutable instantiation of the frozen Hᵁ operator
on B_I, producing realized basis responses

Module J:
primordial covariance, stochastic realization,
linear spectra, growth, and realization fields
```

Therefore:

```text
Module G source history != linear transfer operator
Module Hᵁ operator != realized cosmological background
Module I background != permission to redesign Module H
Module H[I] basis response != realized primordial spectrum
transfer response != primordial covariance
primary linear response != nonlinear or astrophysical secondary physics
```

---

# 2. Module G scientific export

For each admitted Module G branch `gamma_G` descending from the complete A–F ancestry, define:

```text
P_G->Hᵁ(gamma_G) = (
  Identity_G,
  Coord_G,
  GeometrySchema_G,
  Sector_G,
  Species_G,
  Thermo_G,
  Recomb_G,
  Atomic_G,
  Photon_G,
  Polarization_G,
  Neutrino_G,
  Opacity_G,
  OpticalDepth_G,
  Visibility_G,
  LastScattering_G,
  Drag_G,
  Acoustic_G,
  Diffusion_G,
  Damping_G,
  Collision_G,
  Source_G,
  PerturbationSeed_G,
  MetricInterface_G,
  Dark_G,
  Field_G,
  Sigma_G,
  Memory_G,
  Ancestry_G,
  Restart_G
)
```

where:

- `Identity_G` is universe, cycle, branch, and state identity;
- `Coord_G` is the physical-time, conformal-time, scale, temperature, and internally generated coordinate registry through recombination;
- `GeometrySchema_G` is the manifested geometry/background schema and the coefficient types required by a later realized background;
- `Sector_G` is the ordinary, radiative, compression-relic, and dissipative-tail sector registry;
- `Species_G` is the complete active particle, antiparticle, atomic, ionic, nuclear, photon, neutrino, and effective-species registry;
- `Thermo_G` is the density, pressure, stress, equation-of-state, sound-speed, temperature, and chemical-potential state available through recombination;
- `Recomb_G` is the complete nonequilibrium recombination history;
- `Atomic_G` is the atomic/ionic level, continuum, transition, and population history;
- `Photon_G` is the photon frequency and angular distribution state;
- `Polarization_G` is the generated temperature/polarization source state and scattering-polarization ancestry;
- `Neutrino_G` is the flavor, momentum, angular, and anisotropic-stress state;
- `Opacity_G` is opacity separated by physical process;
- `OpticalDepth_G` is the differential and integrated optical-depth history under the frozen sign convention;
- `Visibility_G` is the normalized primordial visibility history and its moments;
- `LastScattering_G` is the finite photon last-scattering probability structure;
- `Drag_G` is the baryon-drag and photon-baryon momentum-transfer history;
- `Acoustic_G`, `Diffusion_G`, and `Damping_G` are the sound, slip, viscosity, conduction, diffusion, and damping histories;
- `Collision_G` is the complete material collision and interaction registry inherited from Modules C–G;
- `Source_G` is the complete temperature, polarization, velocity, metric-interface, and radiative source history;
- `PerturbationSeed_G` is the scalar, vector, tensor, entropy, compensated, relative-velocity, dark-sector, and RFC-specific perturbation seed interface actually authorized upstream;
- `MetricInterface_G` is the stress-energy, anisotropic-stress, momentum-transfer, and geometric coefficient interface required by the future operator;
- `Dark_G` is the compression-relic and dissipative-tail background/perturbative coupling state;
- `Field_G` is any inherited magnetic, vortical, turbulent, shock, defect, or spatially inhomogeneous state that remains materially active;
- `Sigma_G` is the complete atomic, plasma, opacity, perturbative, branch, and numerical covariance;
- `Memory_G` is the recombination, last-scattering, source, route, event, and recursive-memory state;
- `Ancestry_G` is complete ancestry through Modules F, E, D, C, B, and A;
- `Restart_G` is the complete replayable physical state.

All objects remain branch-specific unless Module G proves physical equivalence.

---

# 3. Conditions for Module Hᵁ admission

Module Hᵁ may begin only on a Module G branch satisfying all of the following:

1. the branch is not obstructed;
2. recombination and atomic populations close over the claimed fidelity domain;
3. isotope, charge, particle, photon, and energy ledgers close;
4. photon and polarization source histories are complete;
5. opacity and optical depth are physically generated rather than assigned;
6. the visibility function is normalized and uncertainty-bearing;
7. photon last scattering and baryon drag remain distinct;
8. collision and scattering ancestry is complete;
9. the active species and sector registry is explicit;
10. perturbation-dependent recombination information is attached;
11. metric, stress-energy, momentum-transfer, and anisotropic-stress interfaces are explicit;
12. compression-relic and dissipative-tail perturbative permissions are explicit;
13. magnetic, vortical, defect, turbulent, shock, and spatial branches are preserved where active;
14. uncertainty, branch identity, route identity, memory, and ancestry remain attached;
15. no public expansion history, transfer function, CMB spectrum, matter spectrum, primordial normalization, Boltzmann output, or likelihood state has entered generation.

A missing material field is a Module G or G→Hᵁ interface defect. Module Hᵁ may not fill it from a public Boltzmann code or a conventional cosmological table.

If no admissible Module G branch exists, the universe is obstructed at the G→Hᵁ boundary.

---

# 4. What Module Hᵁ inherits unchanged

Module Hᵁ inherits without redefining:

- universe and cycle identity;
- Big-Implosion ancestry;
- the physical CMB-surface and recombination histories;
- atomic, ionic, nuclear, particle, photon, neutrino, and sector identities;
- microscopic interaction, collision, and scattering laws;
- free-electron, opacity, optical-depth, visibility, and drag histories;
- finite-width last-scattering structure;
- temperature and polarization source histories;
- photon-baryon acoustic and damping state;
- perturbation-dependent recombination response;
- allowed dark-sector and inherited-field couplings;
- uncertainty, branch, event, route, source, and recursive-memory state.

Module Hᵁ may linearize and organize these objects into a response operator. It may not reset or replace them.

---

# 5. Typed background placeholder

Module Hᵁ does not receive the realized Module I background.

Instead it receives or constructs a typed background placeholder:

```text
B = (
  a(eta),
  H(eta),
  clocks,
  metric and curvature coefficients,
  distances and horizons,
  rho_a(eta),
  p_a(eta),
  w_a(eta),
  c_s,a^2(eta),
  interaction-rate histories,
  dark-sector constitutive histories,
  dimensional normalization maps
)
```

The placeholder defines coefficient types and lawful functional dependence. It does not select realized histories.

Module Hᵁ must define the admissible domain `D_B` before Module I is executed.

---

# 6. What Module Hᵁ must derive

Starting from `P_G->Hᵁ`, Module Hᵁ must derive:

- the complete linear perturbation state;
- scalar, vector, and tensor blocks;
- helicity and parity structure where active;
- gauge definitions and gauge transformations;
- gauge-invariant physical variables;
- linearized geometric evolution and constraint grammar;
- species stress-energy perturbations;
- photon intensity and polarization hierarchies;
- baryon and charged-matter dynamics;
- neutral-matter dynamics;
- neutrino and other free-streaming hierarchies;
- compression-relic perturbations;
- dissipative-tail perturbative interface;
- material visible-dark interactions;
- collision and momentum-transfer operators;
- the complete regular initial-mode basis;
- superhorizon and early-time regular series;
- tight-coupling and other stiff-regime approximations;
- fluid and free-streaming limit maps;
- hierarchy truncation and asymptotic closure;
- a direct hierarchy solution operator;
- line-of-sight source and projection grammar;
- basis-normalized Green-function and transfer-response grammar;
- response-derivative and covariance-propagation grammar;
- the admissible realized-background domain `D_B`;
- uncertainty, pathology, convergence, memory, and lineage structures.

The proof object is the operator family:

```text
B_RFC[B] : deltaX_in -> deltaX(k,eta;B)
```

not a precomputed public transfer table or spectrum.

---

# 7. Forbidden Hᵁ inputs and actions

Before the Hᵁ operator is frozen, Module H may not:

- receive or assume the realized Module I background;
- import a public expansion or distance history;
- import public transfer functions;
- import public CMB or matter spectra;
- import an observed primordial amplitude, tilt, running, correlation, or tensor ratio;
- use observed acoustic scales to choose numerical rules;
- insert a public reionization history;
- replace Module G opacity or recombination histories with public tables;
- merge scalar, vector, and tensor sectors without a derived coupling;
- suppress active dark-sector perturbations because a familiar limit is preferred;
- discard regular isocurvature, compensated, velocity, vector, tensor, memory, or sector-specific modes before proving their inadmissibility;
- merge linearly independent route or mode responses merely because one observable channel looks similar;
- alter the operator after public comparison.

---

# 8. Module Hᵁ frozen export to Module I

At completion of the unit phase, Module Hᵁ must export:

```text
P_Hᵁ->I = (
  B_RFC[B],
  D_B,
  PerturbationState_H,
  ModeBasis_H,
  Gauge_H,
  Constraint_H,
  Conservation_H,
  PhotonHierarchy_H,
  PolarizationHierarchy_H,
  MatterHierarchy_H,
  NeutrinoHierarchy_H,
  DarkHierarchy_H,
  Collision_H,
  SourceGrammar_H,
  InitialSeries_H,
  StiffRegime_H,
  Truncation_H,
  DirectSolver_H,
  LOSGrammar_H,
  GreenSchema_H,
  ResponseDerivativeSchema_H,
  UncertaintyOperator_H,
  BackgroundInsertionMap_H,
  IntrinsicSignature_H,
  Memory_H,
  Ancestry_H,
  FrozenOperatorIdentity_H
)
```

Module I must receive enough information to derive a realized background inside `D_B` and later return `B_I` through the declared insertion map.

Module I may not edit the Hᵁ equation grammar, state dimension, mode basis, gauges, collision forms, hierarchy structure, closure rules, source construction, or scientific error criteria.

---

# 9. Later Module I → H[I] instantiation

After Module I derives the realized background packet `B_I`, the later H[I] phase must perform only:

```text
B_H[I] = Instantiate(B_RFC, B_I)
```

Lawful instantiation may populate:

- realized coefficient histories;
- dimensional normalization maps;
- realized sector background histories;
- realized horizon and curvature histories;
- realized interaction-rate coefficient histories;
- declared interpolation nodes;
- background covariance.

It may not redesign:

- the perturbation-state dimension;
- governing equation grammar;
- initial-mode basis;
- gauge system;
- collision structure;
- hierarchy and truncation laws;
- line-of-sight source grammar;
- uncertainty model;
- intrinsic signature definitions.

If `B_I` lies outside `D_B`, the result is a domain obstruction. The operator is not silently altered to accept the background.

---

# 10. Module H[I] output boundary to Module J

The instantiated operator must eventually export basis-normalized responses including:

- curvature and entropy transfer;
- photon temperature multipoles;
- E- and B-polarization transfer;
- baryon density and velocity;
- total matter density and velocity;
- neutrino and free-streaming moments;
- compression-relic density, velocity, and stress;
- dissipative-tail responses where linearly active;
- metric potentials and derivatives;
- Weyl and linear lensing-source responses;
- linear growth and growth-rate responses;
- radiation-matter cross-responses;
- sector entropy and relative-velocity responses;
- direct hierarchy verification histories;
- line-of-sight source histories and projections;
- Green functions;
- response Jacobians;
- transfer uncertainty and covariance maps;
- complete mode, route, species, source, event, background, and memory lineage.

Module J supplies the primordial covariance and realized stochastic mixture. Module H[I] may not choose that mixture.

---

# 11. Concatenated G→Hᵁ theorem target

The Module G and Module H plans concatenate coherently when the following statement can be earned:

> Module G supplies a complete primordial recombination and physical CMB-surface state whose atomic populations, free-electron history, opacity, optical depth, visibility, last-scattering structure, drag history, photon and polarization sources, perturbative response, dark-sector state, covariance, memory, and ancestry are generated and frozen. Module Hᵁ preserves that parent and derives a complete background-parameterized linear multi-species response operator with explicit gauge, conservation, collision, hierarchy, initial-mode, stiff-regime, line-of-sight, Green-function, uncertainty, and background-domain structure. The operator is frozen before Module I supplies the realized background; Module I may later instantiate it only through the declared map, and Module J may later combine its basis responses only after H[I] is complete.

This boundary prevents Module H from silently assuming the background, primordial spectrum, transfer table, or observational result that later modules are responsible for deriving.