# Module G — Nonequilibrium Recombination Proof

## Theorem

Let `P_F->G` be an admitted, nonobstructed Module F branch with restartable isotope, electron, positron, photon, neutrino, plasma, atomic-seed, opacity-seed, perturbation, field, dark, covariance, memory, ancestry, and coordinate state. Let the enhanced completed Module A kernel and frozen Module C–F microscopic/event laws generate the finite Module G operator `L_G(eta)` defined in `science/NONEQUILIBRIUM_RECOMBINATION.md`.

Then, on every declared finite atomic-radiative refinement and finite primordial recombination interval:

1. the Module G initial-value problem has a unique solution;
2. positivity and normalization are preserved;
3. nuclear identities, electric charge, probability, total represented energy-momentum, branch, memory, and ancestry close;
4. nonequilibrium atomic and ionic populations are exact moments of the same positive state;
5. the free-electron history is generated by the atomic/ionic charge state rather than assigned;
6. forward and reverse transition families share one kernel-derived source law and recover detailed balance on equilibrium subdomains;
7. frequency-, angle-, polarization-, and route-resolved photon transport generates line escape, absorption, redistribution, and feedback;
8. process-resolved opacity generates differential and integrated optical depth;
9. the raw and conditional primordial visibility distributions are nonnegative and exactly normalized with explicit boundary mass;
10. photon last scattering and baryon-drag release are distinct generated probability structures unless their hazards are proved equal;
11. diffusion, damping, temperature, polarization, and recombination-radiation source histories descend from the same physical generator;
12. the resulting `P_G->H^U` is sufficient for Module H to construct its universal transfer operator without reconstructing recombination or importing public CMB inputs.

## Assumptions

The proof assumes:

- the sealed Module F parent is complete over the declared fidelity;
- the active geometry, frequency, angular, polarization, spatial, and atomic supports are finite;
- generated coefficients are piecewise continuous;
- every active route has a nonnegative rate, complete witness, conservation pairing, inverse/conjugate status, and event lift;
- every reduction carries an explicit residual or no-loss reopening relation;
- the declared endpoint lies after the represented primordial scattering interval;
- any dark or field interaction is already authorized upstream.

## Proof

### 1. Existence and uniqueness

On a finite refinement, `L_G(eta)` is a finite matrix superoperator. Piecewise continuity of the generated geometry, transition, transport, and event coefficients implies a unique time-ordered exponential solution on every finite interval.

### 2. Positivity and normalization

The Hamiltonian term is trace preserving. Frequency, angular, polarization, neutrino, and spatial transport are constructed as conservative completely positive finite shift generators. Each event term has Lindblad form with nonnegative generated rate. Therefore the propagator is completely positive and trace preserving, so

```text
rho_G(eta) >= 0
Tr rho_G(eta) = 1.
```

### 3. Conserved ledgers

For every exact conserved operator `Q`, the inherited atomic-radiative Hamiltonian commutes with `Q`, and every conserving event lift preserves its eigenvalue. Cyclicity of trace gives

```text
d Tr(Q rho_G)/deta = 0.
```

Conditional violations receive contributions only from explicitly typed violating routes. Photon emission, absorption, redistribution, and atomic binding transfer energy and momentum between represented carriers; they do not erase them. Record and memory carriers preserve route, branch, event, and ancestry information under reduced descriptions.

### 4. Nuclear identity and free-electron generation

Atomic and ionic transitions alter electronic binding and charge state but preserve each nuclear parent operator `N_A^tot`. Hence its expectation is constant except under an explicit residual nuclear route.

The exact charge ledger relates free electrons and positrons to ionic charges and any declared boundary/other charge carriers. Therefore `n_e(eta)` is an output of the solved population state. Any externally prescribed ionization fraction would violate the sealed parent and charge theorem.

### 5. Population equations

For level number operator `N_i`,

```text
d<N_i>/deta = Tr(N_i L_G[rho_G]).
```

Decomposing `L_G` by event routes yields the exact stoichiometric current equation in the science file. Positivity follows from the underlying positive state, and nuclear/charge identities follow from the route stoichiometry. A rate-equation implementation is therefore a controlled moment representation of the same process.

### 6. Forward/reverse and equilibrium closure

The reverse jump operator is the inherited antiunitary conjugate of the forward adjoint. Both rates arise from the same kernel amplitude, occupation factors, and finite energy-resolution law. On a stationary thermal subdomain, microscopic reversibility gives local detailed balance and makes the generalized Gibbs distribution stationary. Saha-like, Boltzmann-like, Planck-like, and capture/photoionization reciprocity identities are limiting consequences, not imported histories.

### 7. Photon transport

The photon equation is a marginal of the completely positive full evolution. Emission, absorption, scattering, redistribution, geometry transport, feedback, residual injection, and authorized dark/field terms all have explicit source and destination carriers. Therefore frequency, angle, polarization, energy, and ancestry remain typed and reopenable.

### 8. Exact finite line escape

For a locally frozen transient line-transfer block `T_l`, eventual absorption makes `-T_l` invertible with nonnegative fundamental matrix `N_l=(-T_l)^-1`. If `B_l` maps transient cells into absorbing outcomes, then

```text
P_l^abs = B_l N_l.
```

Column conservation of the full absorbing generator gives `1^T P_l^abs = 1^T`. Thus the generated escape, destruction, conversion, and return probabilities are nonnegative and normalized. Any scalar escape law accepted later must reproduce this construction within its declared error domain.

### 9. Opacity and optical depth

Each extinction contribution is a nonnegative event expectation. Their sum is therefore a nonnegative hazard `kappa_G`. Defining

```text
tau(eta) = integral_eta^eta_out kappa_G(u) du
```

gives `tau'=-kappa_G` and `tau(eta_out)=0`. Direct differentiation and independent quadrature must agree within numerical error.

### 10. Visibility normalization

Because

```text
g(eta) = kappa_G(eta) exp[-tau(eta)]
       = d exp[-tau(eta)]/deta,
```

integration gives

```text
integral g deta = 1 - exp[-tau(eta_in)].
```

The complementary boundary mass `exp[-tau(eta_in)]` is retained. Dividing by the represented interaction probability gives an exactly normalized conditional visibility distribution. Nonnegativity follows from `kappa_G>=0`.

### 11. Last-scattering and drag distinction

Photon last scattering uses the total photon extinction hazard. Baryon drag uses the relaxation eigenvalue of the photon-baryon relative-momentum block. These are different projections of the physical generator. They coincide only if the generated operators prove equality. Therefore their probability measures, moments, and surfaces remain distinct by default.

### 12. Diffusion and damping

Partitioning the linearized generator into slow and fast blocks and eliminating the stable fast block gives the Schur complement `A-B D^+ C`. When `-D` is positive on the physical fast subspace, the quadratic transport form `-J^T D^+ J` is positive semidefinite. Its symmetric dissipative part produces nonnegative diffusion/viscosity/conduction coefficients and nonpositive damping real parts.

### 13. Atomic and radiative completeness

Module A promotion adds levels, superlevels, continuum cells, photon cells, angular moments, polarization states, spatial cells, and field/dark carriers only when a new witnessed route or material output opens. Closure is reached when every claimed history and the child packet stabilize under refinement and the omitted-effect bound falls below tolerance. A converged incomplete registry remains a failure.

### 14. Perturbation and covariance propagation

Differentiating the complete Module G evolution gives the tangent system for recombination response. Covariance follows the Jacobian pushforward plus positive process, representation, numerical, and branch contributions. This produces perturbation-dependent ionization, opacity, visibility, drag, damping, and source operators for Module H without fixing recombination to a homogeneous public curve.

### 15. Child completeness

The exported packet contains coordinates, background, level and charge-state histories, free-electron/proton histories, photon and polarization states, temperatures, transition and radiation-transfer histories, process-resolved opacity, optical depth, normalized visibility, finite last-scattering and drag measures, slip/sound/diffusion/damping histories, temperature/polarization source ingredients, recombination radiation, perturbation response, dark/field state, covariance, conservation, memory, ancestry, and restart data.

Therefore Module H can construct and later instantiate its transfer operator without inventing or importing any missing recombination physics.

This proves the theorem within the finite-relational, internal-unit, generated atomic-radiative boundary.

## Falsifiers and obstructions

Module G fails on a branch if any mandatory condition occurs:

- negative atomic, ionic, electron, photon, neutrino, or covariance state;
- normalization loss;
- nuclear-identity, charge, energy, or momentum nonclosure;
- a material legal transition omitted or duplicated;
- forward/reverse inconsistency;
- failed detailed-balance limiting case under its named assumptions;
- line-transfer absorption probabilities outside `[0,1]` or not normalized;
- opacity negativity;
- optical-depth derivative/quadrature disagreement beyond tolerance;
- visibility negativity or normalization failure;
- conflation of photon last scattering with baryon drag without an equality theorem;
- nonpositive diffusion or unstable claimed damping branch;
- missing helium, deuterium, continuum, two-photon, forbidden, feedback, perturbation, field, or dark contribution when material;
- failure of level, frequency, angle, space, timestep, precision, or solver convergence;
- incomplete `P_G->H^U`;
- public recombination or CMB targets used to create, repair, or select the branch.

A failure obstructs the branch. It does not authorize retuning Modules A–F.