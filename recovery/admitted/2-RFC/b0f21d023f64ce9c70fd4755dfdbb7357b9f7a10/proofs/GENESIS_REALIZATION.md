# Module B Big-Implosion and Four-Sector Genesis Theorem

## 1. Theorem scope

Module A is complete and frozen. Module B owns the first physical cosmological event and the complete finite-relational preparticle state exported to Module C.

The typed order is

\[
\mathrm{QV}(\mathrm{CIF})\longrightarrow RFL_A^{\mathrm{pre}}
\xrightarrow{\mathrm{BI}_B}
RFL_B^{\mathrm{phys}}.
\]

Therefore

\[
\text{First Action}\neq\text{Big Implosion},
\qquad
RFL_A^{\mathrm{pre}}\neq RFL_B^{\mathrm{phys}}.
\]

The Big Implosion is not a Big Bang, an expansion inside pre-existing spacetime, a hand-inserted singularity, reheating, or a public-data-selected initial condition. It is the lawful compression of an admitted prephysical relational branch into the first manifested physical relational state.

This theorem closes Module B at the following exact scope:

- finite, connected, witnessed relational support;
- a unique manifested image for each fixed admitted source branch;
- first physical event order and intrinsic clock;
- finite relational geometry/pregeometry, not mature Lorentzian spacetime;
- exact directed continuity and global carrier conservation;
- a complete four-sector genesis partition and initial contracts;
- a positive unit-scale family, not a fabricated unique SI calibration;
- a complete preparticle parent state for Module C.

The graph-resolvent, clock, continuity, projector, compression-relic, and dissipative-tail results are source-derived at their stated discrete scope. The unit-family classification, finite physical-slice typing, successor relation, completed sector contracts, common-ancestry map, and exact B→C state assembly are transparent Module B integration derivations.

---

## 2. Sealed Module A input

For one admitted Module A branch \(\beta\), Module B receives

\[
A^*_{\beta}=
(CIF_A,K_A,\delta,\alpha,\mathbf C_N,L_N,
[r]_{\beta},\mathsf W_{\beta},\mathsf E_{\beta},
\sigma_{\mathrm{prot},\beta},M_{\beta},\Pi_{\beta},\Sigma_{\beta}).
\]

A branch is admissible for a single Big Implosion only when:

1. it is not `OBSTRUCTION`;
2. its finite active support is nonempty and connected;
3. its directed relation weights are finite and nonnegative;
4. its positive connected shadow is witnessed;
5. \(\delta>1\);
6. \(\alpha>0\) when a strictly increasing intrinsic Module B clock is claimed;
7. the pre-Implosion carrier is nonconstant when a nontrivial compression event is claimed;
8. source, route, witness, branch, event, memory, uncertainty, and ancestry identities are complete;
9. no public target selected the branch.

A disconnected support is not silently called one genesis event. It must be connected by a witnessed higher relation or retained as a component branch family. An empty admissible family is a genesis obstruction.

---

## 3. First-cycle and later-cycle source envelope

For the first boundary-cycle universe,

\[
M_{\mathrm{rec},-1}^{\mathrm{latent}}=\varnothing
\]

is declared. No fictional earlier universe is introduced.

For a later cycle \(s\), the admissible source has the typed form

\[
CIF_{\mathrm{eff}}(s)
=
\mathcal C_s\!\left(
CIF_{\mathrm{base}}(s);
M_{\mathrm{rec}}^{\mathrm{latent}}(s-1)
\right).
\]

The four states remain distinct:

\[
CIF_{\mathrm{base}}
\neq M_{\mathrm{rec}}^{\mathrm{latent}}
\neq CIF_{\mathrm{eff}}
\neq RFL_B^{\mathrm{phys}}.
\]

Qualified memory may condition admissible modes, route permissions, phase relations, branch permissions, uncertainty, and scale ancestry. It may not replace CIF, perform QV, or execute the Big Implosion.

Module Q must qualify the prior memory. Module A supplies the source-memory type law. Module B consumes only an admitted effective CIF and performs the physical event.

---

## 4. Big-Implosion variational law

Construct the branch-specific directed relation matrix \(W_{\beta}\) from the witnessed Module A lanes. Preserve it in full. Define only its symmetric conductance shadow by

\[
C_{\beta}=W_{\beta}+W_{\beta}^{\mathsf T},
\qquad
D_{\beta,ii}=\sum_k C_{\beta,ik},
\qquad
L_{\beta}=D_{\beta}-C_{\beta}.
\]

For finite nonnegative connected support, \(L_{\beta}\) is symmetric positive semidefinite and

\[
\ker L_{\beta}=\operatorname{span}\{\mathbf 1\}.
\]

Set

\[
\ell_{\delta}=\frac{1}{\delta-1}>0,
\qquad
Q_{\mathrm{imp},\beta}
=\left(I+\ell_{\delta}L_{\beta}\right)^{-1}.
\]

The Big-Implosion output is

\[
\boxed{x^+_{\beta}=Q_{\mathrm{imp},\beta}x^-_{\beta}}.
\]

Equivalently, \(x^+_{\beta}\) is the unique minimizer of

\[
\mathcal S_{\mathrm{imp},\beta}[y]
=
\frac12\|y-x^-_{\beta}\|^2
+
\frac{\ell_{\delta}}2y^{\mathsf T}L_{\beta}y.
\]

The Hessian

\[
I+\ell_{\delta}L_{\beta}\succ0
\]

is positive definite, so the minimizer is regular and unique for the fixed branch.

If \(L_{\beta}v_k=\lambda_kv_k\), then

\[
Q_{\mathrm{imp},\beta}v_k
=
\frac{1}{1+\ell_{\delta}\lambda_k}v_k.
\]

Thus:

- the constant mode \(\lambda_0=0\) is preserved;
- every nonconstant mode \(\lambda_k>0\) is strictly compressed;
- \(Q_{\mathrm{imp},\beta}\) is positivity-preserving on the admitted nonnegative graph domain;
- exact informational reopening is available:

\[
\boxed{
x^-_{\beta}
=
\left(I+\ell_{\delta}L_{\beta}\right)x^+_{\beta}
}.
\]

The inverse relation is not backward physical time. It is protected-state reopening.

---

## 5. Sole first physical event

Define

\[
\mathrm{BI}_{B,\beta}:
A^*_{\beta}\longmapsto P_{B,0}(\beta).
\]

The event is certified when:

1. the input is prephysical and no physical state exists earlier in the same cycle;
2. the output satisfies the stationary variational equation;
3. every admitted nonconstant mode is compressed;
4. total carrier, protected ancestry, and uncertainty remain accounted for;
5. the output is assigned physical event origin \(t_{\mathrm{phys}}=0^+\);
6. all four sector seeds reopen to this one event;
7. the branch was selected only by source-owned admissibility and witnesses.

For one fixed branch, strict convexity gives one manifested image. A lawful family of physically distinct CIF branches remains a branch family; it is not averaged or collapsed to imitate observation.

The Big Implosion is therefore the sole **event type** crossing from the prephysical Module A state to physical chronology, with one unique image per admitted fixed branch or a preserved lawful branch family.

---

## 6. First manifested physical state

For each admitted branch \(\beta\), define

\[
P_{B,0}(\beta)=
(G_B,\Phi_B,J_B,T_B,S_B,
X_{\mathrm{ord}},X_{\mathrm{rad}},X_{\mathrm{comp}},X_{\mathrm{tail}},
\mathrm{Sym}_B,\mathrm{Br}_B,U_B,\Sigma_B,M_B,A_B).
\]

Its components are:

### 6.1 Relational geometry/pregeometry \(G_B\)

\[
G_B=(V_{\beta},W_{\beta},C_{\beta},L_{\beta},L_{\beta}^{+},d_R,\Sigma_{B,0}^{\mathrm{rel}}),
\]

where

\[
d_R(i,k)
=(e_i-e_k)^{\mathsf T}L_{\beta}^{+}(e_i-e_k).
\]

On connected finite support, \(d_R\) is a genuine positive relational metric. It measures coupling/resistance separation in the manifested finite slice. It is not a Lorentzian interval and supplies no timelike, spacelike, or null classification.

The first slice \(\Sigma_{B,0}^{\mathrm{rel}}\) is the manifested finite vertex-relation state at \(t_{\mathrm{phys}}=0^+\). Its continuum dimension and mature metric status remain unresolved and are passed downstream.

### 6.2 Manifested carrier \(\Phi_B\)

\[
\Phi_B=(x^-_{\beta},x^+_{\beta},\bar x\mathbf1,x^{\perp}_{\beta}),
\qquad
\bar x=\frac1N\mathbf1^{\mathsf T}x^+_{\beta}.
\]

The post-Implosion carrier \(x^+\), not merely the operator, is the first manifested field/carrier state.

### 6.3 Directed current state \(J_B\)

For every directed lane,

\[
j_{(i\mid k)}
=
\ell_{\delta}w_{(i\mid k)}(x_i^+-x_k^+).
\]

For each unordered pair,

\[
F_{ik}=j_{(i\mid k)}-j_{(k\mid i)},
\qquad
F_{ik}=-F_{ki}.
\]

The full directed \(j\) state remains present. \(F\) is its antisymmetric net-flux shadow.

### 6.4 Clock state \(T_B\)

\[
T_B=(t_{\mathrm{phys}}=0^+,\nu,\tau_B,t_B),
\]

where \(\nu\) is cumulative witnessed compression progress, \(\tau_B\) is the intrinsic dimensionless clock, and \(t_B>0\) is a unit conversion rather than a dynamical fit parameter.

### 6.5 Genesis law and ledger \(S_B\)

\[
S_B=(\mathcal S_{\mathrm{imp}},Q_{\mathrm{imp}},
E^-,E^+,D_{\mathrm{QV}},\mathcal C_{\mathrm{loc}},\mathcal C_{\mathrm{glob}}).
\]

It includes the variational law, compression budget, local continuity, and global conservation.

### 6.6 Remaining state

The four \(X\) objects are the exact seed sectors below. \(\mathrm{Sym}_B\) preserves event and route involutions plus any source-owned orientation. \(\mathrm{Br}_B\) preserves route/gauge/multiroute status. \(U_B\) is the scale family. \(\Sigma_B\) is full uncertainty and covariance. \(M_B\) is protected reopening memory. \(A_B\) is complete Module A and Big-Implosion ancestry.

No particle catalogue, photon, baryon, dark-matter particle, dark-energy equation of state, thermal history, or mature spacetime is contained in this tuple.

---

## 7. Continuity, conservation, and compression

The Big-Implosion equation

\[
(I+\ell_{\delta}L)x^+=x^-
\]

is equivalent vertex by vertex to

\[
\boxed{
x_i^+-x_i^-+\sum_kF_{ik}=0
}.
\]

Because \(F_{ik}=-F_{ki}\), summing over vertices gives

\[
\boxed{
\sum_i x_i^+=\sum_i x_i^-
}.
\]

Define the nonconstant carrier energy

\[
E(x)=\frac12\|P_{\perp}x\|^2,
\qquad
P_{\perp}=I-\frac1N\mathbf1\mathbf1^{\mathsf T}.
\]

For a nonconstant admitted branch,

\[
E^+=E(x^+)<E(x^-)=E^-.
\]

The exact compression/storage ledger is

\[
\boxed{
D_{\mathrm{QV}}=E^--E^+>0,
\qquad
E^-=E^++D_{\mathrm{QV}}.
}
\]

\(D_{\mathrm{QV}}\) is an exact genesis compression quantity. It is not automatically heat, radiation, rest mass, dark matter, dark energy, or continuum stress-energy.

A discrete current/storage precursor can be passed downstream, but a continuum tensor \(T_{\mu\nu}\) is not asserted in Module B.

---

## 8. Intrinsic physical event order and clock

For a witnessed sequence of nontrivial finite genesis compressions

\[
x_{r+1}=Q_rx_r,
\qquad
Q_r=(I+\ell_{\delta}L_r)^{-1},
\]

define

\[
\chi_r=-\log\frac{E_{r+1}}{E_r}>0,
\qquad
\nu_m=\sum_{r=0}^{m-1}\chi_r.
\]

A physical successor event is admitted only when:

1. it is generated from an admitted physical parent by the Module B law;
2. \(\chi_r>0\);
3. the route-resolved continuity residual vanishes within tolerance or exact arithmetic;
4. global carrier conservation holds;
5. branch, event, and protected ancestry are retained;
6. any topology change is separately witnessed by the Module A event grammar.

Define

\[
q(u)=\frac{e^{-\alpha u}}{\delta},
\qquad
p_j(u)=(1-q)q^j,
\]

and

\[
S_{\mathrm{rec}}(u)
=-\log(1-q)-\frac{q\log q}{1-q}.
\]

For \(\alpha>0\), \(\delta>1\), and \(u\ge0\),

\[
\frac{dS_{\mathrm{rec}}}{du}
=-\frac{\alpha\delta e^{\alpha u}(\alpha u+\log\delta)}
{(\delta e^{\alpha u}-1)^2}<0.
\]

Choose the Big-Implosion origin \(u_{\mathrm{imp}}\). The intrinsic clock is

\[
\boxed{
\tau_B(u;u_{\mathrm{imp}})
=
\log\frac{S_{\mathrm{rec}}(u_{\mathrm{imp}})}{S_{\mathrm{rec}}(u)}
}.
\]

It satisfies:

- \(\tau_B(u_{\mathrm{imp}})=0\);
- \(d\tau_B/du>0\);
- \(\tau_B\to\infty\) as \(u\to\infty\);
- it induces the same order as positive cumulative compression progress;
- it does not identify depth \(j\), ancestry rank, route number, or cycle number with time.

Physical time is the unit family

\[
\boxed{t_{\mathrm{phys}}=t_B\tau_B,\qquad t_B>0.}
\]

Changing \(t_B\) changes units, not event order or dynamics. If \(\alpha=0\), this clock does not progress and the branch fails the Module B intrinsic-clock condition.

---

## 9. Dimensional scale-family theorem

The source equations determine dimensionless relational ratios and event order but do not determine a unique SI calibration.

Module B therefore exports the positive unit family

\[
U_B=(t_B,\ell_B,\phi_B,\mathcal E_B),
\qquad
t_B,\ell_B,\phi_B,\mathcal E_B>0,
\]

with

\[
t_{\mathrm{phys}}=t_B\tau_B,
\qquad
d_B(i,k)=\ell_Bd_R(i,k),
\qquad
\Phi_B^{\mathrm{phys}}=\phi_Bx^+,
\qquad
\mathcal E^{\mathrm{phys}}=\mathcal E_BE.
\]

All dimensionless identities, branch classifications, continuity equations, projector identities, and ancestry relations are invariant under positive changes of these unit representatives.

This closes Module B dimensionalization honestly:

- the scale **structure and freedom** are derived;
- no unique SI values are fabricated;
- unit-equivalent representatives are not new physical branches;
- a later source-derived microscopic or geometric law may reduce the scale family;
- public data may not choose the representative during generation.

---

## 10. Exact four-sector seed partition

Let the complete genesis carrier be the direct sum

\[
\mathcal H_B
=
(\mathcal H_x\oplus\mathcal H_x)
\oplus
(\mathcal H_j\oplus\mathcal H_j),
\]

with state pair \((x^-,x^+)\) and aligned forward/reverse current pair \((j_{\rightarrow},j_{\leftarrow})\).

Define the event swap on the state block,

\[
\mathsf E(a,b)=(b,a),
\]

and the route swap on the current block,

\[
\mathsf R(c,d)=(d,c).
\]

Both are self-adjoint involutions. Define

\[
P_{\mathrm{ord}}
=\frac{I+\mathsf E}{2}\oplus0,
\qquad
P_{\mathrm{comp}}
=\frac{I-\mathsf E}{2}\oplus0,
\]

\[
P_{\mathrm{rad}}
=0\oplus\frac{I-\mathsf R}{2},
\qquad
P_{\mathrm{tail}}
=0\oplus\frac{I+\mathsf R}{2}.
\]

They satisfy

\[
\boxed{
\sum_aP_a=I,
\qquad
P_aP_b=\delta_{ab}P_a,
\qquad
P_a^{\mathsf T}=P_a.
}
\]

The reduced seed representatives are

\[
o_0=\frac{x^-+x^+}{2},
\qquad
c_0=\frac{x^--x^+}{2}
=\frac{\ell_{\delta}}2Lx^+,
\]

\[
r_0=\frac{j_{\rightarrow}-j_{\leftarrow}}2,
\qquad
t_0=\frac{j_{\rightarrow}+j_{\leftarrow}}2.
\]

Thus:

- \(X_{\mathrm{ord}}\) is event-even;
- \(X_{\mathrm{comp}}\) is event-odd;
- \(X_{\mathrm{rad}}\) is route-odd;
- \(X_{\mathrm{tail}}\) is route-even.

The complete state reopens exactly:

\[
Y_B=(x^-,x^+;j_{\rightarrow},j_{\leftarrow})
=
\sum_aP_aY_B.
\]

Norms decompose orthogonally. Covariance reopens with all diagonal and cross blocks retained:

\[
\Sigma_Y
=
\sum_{a,b}P_a\Sigma_YP_b.
\]

No residual is hidden and no contribution is counted twice. Orthogonality of the state/current decomposition does not imply all stochastic cross-covariances vanish.

---

## 11. Four genesis-sector contracts

### 11.1 Ordinary seed

\[
X_{\mathrm{ord}}=(o_0,\text{event-even ancestry},\Sigma_{\mathrm{ord}},\mathsf{permissions}).
\]

It carries the event-even manifested carrier, total-carrier ancestry, initial localization support, allowed interaction-visible degrees of freedom, coupling permissions, branch state, and uncertainty.

It is not yet a particle catalogue, baryons, electric charge, or ordinary matter in the mature empirical sense.

### 11.2 Radiative seed

For one aligned unordered pair,

\[
r_{ik}
=\frac{j_{(i\mid k)}-j_{(k\mid i)}}2
=\frac{F_{ik}}2.
\]

The radiative contract carries orientation, route-odd propagation capacity, current support, coupling permissions, covariance, and ancestry.

It is not yet a photon, electromagnetism, a massless gauge field, or measured radiation.

### 11.3 Compression-relic seed

The initial relic is

\[
\boxed{
c_0=\frac{x^--x^+}{2}=\frac{\ell_{\delta}}2Lx^+.}
\]

Because \(c_0\perp\mathbf1\), its discrete persistence law is

\[
c_r=Q_{\mathrm{imp}}^rc_0.
\]

Define

\[
G_{\mathrm{comp}}
=\log(I+\ell_{\delta}L)
=-\log Q_{\mathrm{imp}}\succeq0.
\]

Then

\[
\boxed{
c(\tau)=e^{-\tau G_{\mathrm{comp}}}c_0.}
\]

On connected support, every nonconstant spectral component is non-growing and decays asymptotically. At every finite \(\tau\), exact algebraic reopening is

\[
c_0=e^{\tau G_{\mathrm{comp}}}c(\tau),
\]

with conditioning reported rather than hidden.

For fixed \(G_{\mathrm{comp}}\), covariance propagates as

\[
\Sigma_c(\tau)
=e^{-\tau G_{\mathrm{comp}}}
\Sigma_{c,0}
e^{-\tau G_{\mathrm{comp}}}.
\]

This is an initial persistence and compression-ancestry contract. It does not establish particle mass, pressure, halo formation, clustering, or lensing.

### 11.4 Dissipative-tail seed

For an unordered pair with aligned orientation,

\[
t_{ik}
=\frac{j_{(i\mid k)}+j_{(k\mid i)}}2
=
\frac{\ell_{\delta}}2
(w_{ik}-w_{ki})(x_i^+-x_k^+).
\]

Therefore:

- \(t_{ik}\) is route-even;
- it is invisible to \(F_{ik}=2r_{ik}\);
- it vanishes on exactly reciprocal routes \(w_{ik}=w_{ki}\);
- it measures directed nonreciprocal route content rather than net antisymmetric transport.

Define the native magnitude and geometry-load ledgers on active edges:

\[
\mu_{\mathrm{tail}}=\sum_{i<k}|t_{ik}|,
\qquad
\Gamma_{\mathrm{tail}}
=\frac12\sum_{i<k:C_{ik}>0}
\frac{t_{ik}^2}{C_{ik}}\ge0.
\]

The preterminal contract admits a branch-derived positive-semidefinite generator \(G_{\mathrm{tail}}\) only when it preserves the route-even subspace and satisfies the inherited witness conditions. Then

\[
t(\tau)=e^{-\tau G_{\mathrm{tail}}}t_0
\]

is non-growing, and its covariance propagates under the same semigroup plus declared model/numerical uncertainty.

The terminal interface is exported dormant. Module I must derive any large-scale geometry effect or pressure law. Module Q must derive any Big-Rip activation. Module B does not assign an equation of state or acceleration.

---

## 12. Symmetry, branch, and asymmetry state

The exact Module B symmetries are:

- event swap on \((x^-,x^+)\);
- route swap/reversal on aligned directed currents;
- graph automorphisms that preserve the complete protected signature;
- Module A representative and gauge equivalences.

Define the directed-orientation tensor

\[
A_W=W-W^{\mathsf T}.
\]

\(A_W\), \(r_0\), \(t_0\), and lawful source phases may supply genesis orientation or asymmetry seeds.

They are not particle chirality, CP violation, baryon asymmetry, or an observed matter-antimatter imbalance. Module C and Module D must derive those physical meanings.

For fixed \(\beta\), the Big-Implosion image is unique. Distinct protected source branches remain distinct. Gauge-equivalent representatives are quotiented only under the frozen Module A no-loss rule.

---

## 13. Common cosmogenic ancestry theorem

Every sector is obtained by an exact projector from the same complete genesis carrier:

\[
X_a=P_aY_B.
\]

The ancestry chain is

\[
X_a
\longrightarrow Y_B
\longrightarrow P_{B,0}(\beta)
\longrightarrow\mathrm{BI}_{B,\beta}
\longrightarrow CIF_{\mathrm{eff},\beta}
\longrightarrow A^*_{\beta}.
\]

Because \(\sum_aP_a=I\), all four seeds reopen to one \(Y_B\). Because \(x^-=(I+\ell_{\delta}L)x^+\), the manifested state reopens to the protected pre-event carrier. The Module A memory map then reopens the protected parent signature.

Therefore the four seeds are differentiated descendants of one Big Implosion and one admitted source branch. They are not independent substances added later.

---

## 14. Uncertainty and covariance

For fixed \(Q_{\mathrm{imp}}\),

\[
\Sigma_{x^+}
=Q_{\mathrm{imp}}\Sigma_{x^-}Q_{\mathrm{imp}}^{\mathsf T}.
\]

With uncertain relation weights, source parameters, scale promotion, branch choice, and numerical approximation,

\[
\Sigma_{B}
=J_B\Sigma_{A}J_B^{\mathsf T}
+\Sigma_{W}
+\Sigma_{\mathrm{scale}}
+\Sigma_{\mathrm{branch}}
+\Sigma_{\mathrm{model}}
+\Sigma_{\mathrm{numeric}}.
\]

Branch uncertainty is retained as a lawful family, not replaced by one averaged universe unless a later theorem defines that mixture physically.

The exact four-sector covariance record is

\[
\Sigma_{ab}=P_a\Sigma_YP_b,
\qquad a,b\in
\{\mathrm{ord},\mathrm{rad},\mathrm{comp},\mathrm{tail}\}.
\]

No sector covariance is rebuilt from public data.

---

## 15. Frozen primordial signatures

Module B freezes only internal signatures generated before public comparison:

- the Laplacian spectrum \(\{\lambda_k\}\);
- compression factors \((1+\ell_{\delta}\lambda_k)^{-1}\);
- \(D_{\mathrm{QV}}\) and \(\chi_r\) distributions;
- the directed asymmetry \(A_W\);
- event/route parity sector weights;
- all sector covariance blocks;
- compression-relic/tail cross-covariance;
- effective-resistance topology;
- branch multiplicity and protected ancestry;
- recursive-depth correlations inherited from Module A.

These are carried forward as predictions or branch signatures. Module B performs no public-data comparison.

---

## 16. Complete Module C parent state

The exact Module B export is

\[
\boxed{
P_{B\to C}(\beta)=
(G_B,\Phi_B,J_B,T_B,S_B,
X_{\mathrm{ord}},X_{\mathrm{rad}},X_{\mathrm{comp}},X_{\mathrm{tail}},
\mathrm{Sym}_B,\mathrm{Br}_B,U_B,\Sigma_B,M_B,A_B).
}
\]

It includes:

- the first manifested carrier;
- finite relational geometry/pregeometry and its limitation;
- physical event origin and intrinsic clock;
- directed current and net-flux state;
- local continuity and global conservation;
- compression/storage ledger;
- all four complete seed contracts;
- symmetry and lawful asymmetry seeds;
- carrier/energy constraints and charge-capacity permissions without electric charge already assigned;
- scale freedom;
- branch family;
- complete covariance;
- protected memory and ancestry;
- microscopic activation permissions.

It contains no fabricated particle species, measured masses, observed abundances, imported thermal history, BBN, recombination, halos, stars, mature observables, or public targets.

Module C must derive particles, fields, charges, probabilities, interactions, masses, hadrons, photons, neutrinos, matter-antimatter structure, and the prethermal baryon-photon state from this parent.

---

## 17. Obligation closure

| ID | Module B result | Closure |
|---|---|---:|
| B1 | Big-Implosion event law | `PROVED` on finite connected admitted branches |
| B2 | Sole first physical event, order, and intrinsic clock | `PROVED` for \(\alpha>0\) and nontrivial witnessed compression |
| B3 | Dimensions and scale ancestry | `CLOSED_AS_POSITIVE_UNIT_FAMILY`; no unique SI claim |
| B4 | First manifested state variables | `CONSTRUCTED_AND_TYPED` |
| B5 | Post-event genesis evolution | `PROVED_WITHIN_FINITE_WITNESSED_COMPRESSION_DOMAIN` |
| B6 | Local continuity and global conservation | `PROVED` |
| B7 | Refinement/validity | `FINITE_RELATIONAL_REGIME_DECLARED`; continuum claim deferred |
| B8 | Four-sector differentiation and contracts | `PROVED_AT_SEED_SCOPE` |
| B9 | Complete preparticle export to C | `CONSTRUCTED_AND_SEALED` |

---

## 18. Exact claim boundary

Module B proves, at the declared finite-relational scope:

- the Big Implosion as the sole first physical event type;
- one unique manifested image for each fixed admitted source branch;
- strict nonconstant-mode compression and constant-carrier preservation;
- exact protected reopening;
- a finite physical relational slice and resistance geometry;
- intrinsic event order and clock up to positive unit scale;
- exact directed continuity and global carrier conservation;
- exact nonduplicated four-sector seed partition;
- ordinary, radiative, compression-relic, and dissipative-tail genesis contracts;
- common Big-Implosion ancestry;
- branch-complete uncertainty and covariance;
- the complete preparticle B→C parent state.

Module B does not prove:

- a Big Bang;
- pre-existing spacetime;
- a unique SI calibration;
- mature Lorentzian geometry, curvature, horizons, redshift, or expansion;
- a continuum stress-energy tensor;
- particle, photon, baryon, neutrino, or gauge identities;
- physical dark-matter halo/lensing behavior;
- physical dark-energy pressure or acceleration;
- the Big Rip;
- empirical agreement.

Within those boundaries, Module B is complete and frozen. Module C is its proper active child.