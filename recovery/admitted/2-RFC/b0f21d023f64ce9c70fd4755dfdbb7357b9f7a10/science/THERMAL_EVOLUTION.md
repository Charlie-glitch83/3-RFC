# Module D — Triadic Nonequilibrium Thermal Evolution

## 1. Scientific boundary

Modules A, B, and C are complete and frozen. Module D consumes the sealed prethermal microscopic state

\[
P_{C\to D}(\beta,\gamma)
=
(G_C,T_C,X_C,F_C,R_C,M_C,Q_C,V_C,A_C,B_C,N_C,L_C,
Y_C,P_C,E_C,D_C,\Sigma_C,M_{\mathrm{rec},C},\mathrm{Anc}_C)
\]

and derives its complete early thermal history without altering the microscopic parent.

The completed Module D theorem is:

> For every admitted finite-relational Module C branch, the enhanced completed Module A triad kernel, the Module C many-body generator and complete event grammar, and the inherited Big-Implosion clock and geometry generate a unique branch-specific nonequilibrium thermal evolution. The evolution preserves positivity, normalization, all exact ledgers, route and event ancestry, branch identity, recursive memory, and uncertainty; derives equilibrium only where the dynamics earns it; generates finite-temperature phase structure, asymmetry transport, annihilation, freeze-out, decoupling, neutrino transport, photon heating, visible-dark exchange, plasma response, and the complete nuclear-ready state required by Module E.

The claim is made at a **finite-relational, internal-unit, exact-generator scope**. It does not import a standard hot-universe timeline, measured transition temperature, observed baryon ratio, conventional neutrino temperature ratio, public equation-of-state table, or public nuclear initial condition.

---

## 2. The inherited enhanced triad kernel

Every Module D construction uses the full Module A kernel rather than a generic or reduced triad reference.

Let

\[
\widehat{\mathcal K}_{A}(\tau)
=
\sum_{j=1}^{n_A}
\sum_{[r]\in\mathcal A_A(\tau)}
\delta^{-j}e^{-\alpha j\tau}
W_{j,[r]}(\tau)\Phi_{j,[r]}(\tau)
\]

carry:

- the ordered CIF/QV/RFL roles;
- the First Action \(QV(CIF)\to RFL\);
- recursive depth and damping;
- directed relations and route classes;
- local witnesses;
- event lift;
- branch and obstruction law;
- no-loss quotienting;
- identity-preserving memory;
- promotion and reopening;
- uncertainty and covariance;
- terminal N-body relational completion.

The N-body carrier is active wherever a thermal process requires represented constituents, directed influence, witnessed routes, collision or branch events, conservation, or refinement. Where no such realization exists, the carrier remains dormant, information-bearing, unchanged, and nonbackreacting.

---

## 3. Thermal-domain triad

The Module D specialization is

\[
CIF_D
=\Phi_D(P_{C\to D},\widehat{\mathcal K}_{A},M_{\mathrm{rec},C}),
\]

\[
QV_D
=\Psi_D(CIF_D,P_{C\to D},\widehat{\mathcal K}_{A},\mathfrak E_C),
\]

\[
RFL_D
=\Omega_D(QV_D,P_{C\to D},\widehat{\mathcal K}_{A}),
\]

\[
H_D
=\Gamma_D(CIF_D,QV_D,RFL_D,P_{C\to D},\widehat{\mathcal K}_{A}).
\]

Here \(\mathfrak E_C\) is the complete Module C microscopic event family.

The roles are physical and indispensable:

- **CIF\(_D\)** opens every lawful thermal configuration, channel, coherence, phase, transport route, and sector transfer permitted by the frozen parent;
- **QV\(_D\)** executes microscopic events, collisions, propagation, phase conversion, charge conversion, annihilation, transport, decoherence, and lawful branch resolution;
- **RFL\(_D\)** stabilizes distributions, local-equilibrium manifolds, phase populations, relic survivors, decoupled states, memory, and the nuclear-ready export.

The triad is not replaced by three scalar thermal fields.

---

## 4. Finite thermal configuration space

Module C supplies a finite relational Fock/state space, a finite species registry, a finite geometry, an exact statistics grading, a complete interaction grammar, and a branch-specific internal energy scale.

Choose one declared finite refinement \(\mathfrak R_D\) of the inherited geometry and momentum/route support. Define

\[
\Omega_D(\mathfrak R_D)
=
\{\omega\}
\]

as the set of all legal many-body configurations satisfying:

- Module C state and statistics rules;
- finite energy and occupancy cutoff inherited from the branch;
- exact charge and sector typing;
- legal-incidence closure;
- no duplicate no-loss route representatives;
- complete event ancestry.

A configuration records, where applicable:

\[
\omega=
(n_{a\kappa},\rho_{\nu\kappa},G,\mathcal B,\mathcal S,
M_{\mathrm{rec}},\mathrm{Anc}),
\]

where \(a\) is a species or collective mode and \(\kappa\) is a finite relational cell/momentum-mode label.

The full thermal state is a positive density operator

\[
\rho_D(\tau)\succeq0,
\qquad
\operatorname{Tr}\rho_D(\tau)=1
\]

on the finite configuration space. Classical configuration probabilities are its diagonal restriction.

Refinement enlarges \(\Omega_D\) only through the frozen Module A add-one, promotion, and reopening laws. It may not insert a conventional species or process.

---

## 5. Exact many-body generator

Let

\[
\mathbb H_C(G)
\]

be the frozen Module C many-body Hamiltonian assembled from its one-body spectra, exact invariant vertices, bound-state operators, memory terms, and geometry dependence.

Module D does not append a thermal potential by hand. Medium effects are generated by the same finite many-body operator acting on a nonvacuum state.

For every admitted microscopic route \(r:i\to f\), define the triad-kernel amplitude

\[
\mathcal A_r(\tau)
=
\sum_{j}
\delta^{-j}e^{-\alpha j\tau}
W_{j,r}(\tau)
\langle f|\Phi_{j,r}|i\rangle.
\]

Let \(P_i,P_f\) be the protected input and output projectors. The normalized event-lift operator is

\[
J_r
=
\frac{P_fV_rP_i}
{\|P_fV_rP_i\|}
\]

when the denominator is nonzero. A zero denominator obstructs that channel.

The positive route rate is

\[
\gamma_r(\tau)
=
\frac{1}{t_B}
|\mathcal A_r(\tau)|^2
\Delta_{\sigma_r}
\bigl(E_f(\tau)-E_i(\tau)\bigr),
\]

where \(t_B>0\) is the inherited physical-time unit and \(\Delta_{\sigma_r}\) is the normalized finite-spectrum resolution kernel already authorized by Module C.

No measured cross section or transition temperature enters \(\gamma_r\).

Creation, annihilation, Pauli blocking, bosonic enhancement, multiplicity, and inverse-channel factors follow from the Module C operator algebra and the occupation matrix elements of \(J_r\). They are not separately fitted factors.

---

## 6. Exact thermal evolution law

The reduced thermal state evolves by

\[
\boxed{
\frac{d\rho_D}{d\tau}
=
\mathcal L_D(\tau)[\rho_D]
=-i[\mathbb H_C(G_D),\rho_D]
+\sum_{r\in\mathcal E_D}
\gamma_r(\tau)
\left(
J_r\rho_DJ_r^\dagger
-\frac12\{J_r^\dagger J_r,\rho_D\}
\right).
}
\]

The record and memory degrees omitted from the reduced state retain the complete event output, so the full parent-plus-record evolution remains no-loss. The dissipative term describes the reduced state after lawful record stabilization, not fundamental deletion.

For piecewise continuous \(G_D\) and rates, the exact solution is

\[
\boxed{
\rho_D(\tau)
=
\mathcal T
\exp\left[
\int_{0}^{\tau}\mathcal L_D(u)\,du
\right]
\rho_D(0).
}
\]

Because the represented state space is finite, this solution exists uniquely over every finite declared interval.

### 6.1 Classical configuration restriction

On diagonal states \(p_\omega=\langle\omega|\rho_D|\omega\rangle\),

\[
\dot p=Q_Dp,
\]

with

\[
(Q_D)_{\omega'\omega}
=
\sum_{r:\,r(\omega)=\omega'}\gamma_r
\quad(\omega'\ne\omega),
\]

\[
(Q_D)_{\omega\omega}
=-\sum_{\omega'\ne\omega}(Q_D)_{\omega'\omega}.
\]

Thus every off-diagonal entry is nonnegative and every column sums to zero.

---

## 7. Positivity, normalization, probability, and no-loss theorem

For the exact Module D generator:

1. \(\rho_D(\tau)\succeq0\) whenever \(\rho_D(0)\succeq0\);
2. \(\operatorname{Tr}\rho_D(\tau)=1\);
3. every classical marginal remains nonnegative and normalized;
4. reverse routes remain explicit whenever the frozen microscopic law permits them;
5. every annihilation, decay, conversion, or phase event retains an output carrier and event-memory record;
6. exact conserved operators remain conserved.

For any operator \(Q_A\) satisfying

\[
[Q_A,\mathbb H_C]=0,
\qquad
[Q_A,J_r]=0
\]

for every active route,

\[
\frac{d}{d\tau}\operatorname{Tr}(Q_A\rho_D)=0.
\]

For conditionally violated charges, only the declared violating jump family contributes.

This closes probability, electric charge, exact baryon/lepton combinations, energy including geometry, and every branch-specific invariant at its declared scope.

---

## 8. Species distributions and exact collision operators

For each species or mode \((a,\kappa)\), define

\[
f_{a\kappa}(\tau)
=\operatorname{Tr}\bigl(\rho_Dn_{a\kappa}\bigr).
\]

Its exact evolution is

\[
\dot f_{a\kappa}
=\operatorname{Tr}
\left(n_{a\kappa}\mathcal L_D[\rho_D]\right).
\]

For a reaction route with stoichiometric vector \(\nu_r\), the species-number moment is

\[
\dot N_a
=\sum_r\nu_{ar}\,\mathcal J_r,
\]

where the net event current is

\[
\mathcal J_r
=
\gamma_r
\operatorname{Tr}(J_r^\dagger J_r\rho_D)
-
\gamma_{\bar r}
\operatorname{Tr}(J_{\bar r}^\dagger J_{\bar r}\rho_D).
\]

Every legal Module C channel appears once after the no-loss route quotient. Every thermal inverse channel retains its own route identity. Missing or duplicated material channels are obstructions.

The usual distribution-level collision term is therefore a marginal of the exact finite configuration-space law, not an imported Boltzmann equation.

---

## 9. Physical time, recursive depth, scale, and temperature

The variables remain distinct:

\[
t_{\mathrm{phys}}=t_B\tau,
\qquad
j=\text{recursive depth},
\qquad
s=\text{cycle index}.
\]

Recursive depth is not physical time.

Temperature is not imposed on the prethermal state. For a chosen subalgebra \(\mathfrak A\), define the generalized Gibbs family

\[
\rho_{\mathrm{G}}(\beta,\boldsymbol\mu;G)
=
\frac{
\exp[-\beta(\mathbb H_C(G)-\sum_A\mu_AQ_A)]
}{Z_D(\beta,\boldsymbol\mu;G)},
\]

\[
Z_D
=\operatorname{Tr}
\exp[-\beta(\mathbb H_C-\sum_A\mu_AQ_A)].
\]

A scalar temperature exists only when \((\beta,\boldsymbol\mu)\) can match the actual energy and charge moments and the information-projection residual is sufficiently small.

Define

\[
\mathcal R_{\mathrm{eq}}
=
D\bigl(\rho_D\|\rho_{\mathrm{G}}^*\bigr),
\]

where \(\rho_{\mathrm{G}}^*\) is the moment-matching generalized Gibbs state. Then

- \(T=\beta^{-1}\) is meaningful when \(\mathcal R_{\mathrm{eq}}\) is below the declared physical tolerance;
- different sectors may have different \(\beta_a\);
- a state with no satisfactory projection remains explicitly nonequilibrium.

The temperature unit is inherited from the Module C internal energy scale \(\Lambda_C\). No measured Kelvin calibration is required for the dimensionless thermal theorem.

---

## 10. Equilibrium, detailed balance, and entropy production

A route-paired state satisfies microscopic detailed balance when

\[
\gamma_r p_{\omega}^{\mathrm{eq}}
=
\gamma_{\bar r}p_{\omega'}^{\mathrm{eq}}
\]

for every connected pair \(\omega\leftrightarrow\omega'\).

For the classical restriction, the thermodynamic entropy-production rate is

\[
\sigma_{\mathrm{th}}
=
\frac12
\sum_{\omega,\omega'}
\left(
J_{\omega'\omega}-J_{\omega\omega'}
\right)
\log
\frac{J_{\omega'\omega}}{J_{\omega\omega'}}
\ge0.
\]

The inequality follows termwise from the monotonicity of \(\log x\).

Consequently the relative entropy to a detailed-balance stationary state is nonincreasing. Equilibrium is therefore an RFL-stabilized attractor when the route graph is connected and the active rates remain nonzero.

Kinetic equilibrium, chemical equilibrium, partial equilibrium, recoupling, and departure from equilibrium are classified by:

- information-projection residual;
- detailed-balance residual;
- relevant Liouvillian spectral gaps;
- charge-chemical-potential closure;
- distribution-shape residual;
- transport mean free path relative to relational scale.

Equilibrium is earned dynamically.

---

## 11. Thermodynamics and equation of state

Define

\[
E_D=\operatorname{Tr}(\rho_D\mathbb H_C),
\]

\[
S_{\mathrm{th},D}
=-\operatorname{Tr}(\rho_D\log\rho_D),
\]

and let \(V_D(G_D)>0\) be the inherited relational volume functional.

On a local-equilibrium branch,

\[
F_D
=-\beta^{-1}\log Z_D,
\qquad
P_D
=-\left.\frac{\partial F_D}{\partial V_D}\right|_{\beta,\mu},
\]

\[
\rho_D^{E}=E_D/V_D,
\qquad
h_D=\rho_D^{E}+P_D,
\qquad
c_s^2=\frac{dP_D}{d\rho_D^{E}}
\]

along the physical branch.

Outside equilibrium, pressure and stress are computed directly from the geometry derivative

\[
\Pi_D
=-\operatorname{Tr}
\left(\rho_D\frac{\partial\mathbb H_C}{\partial\log G_D}\right),
\]

rather than from an equilibrium equation of state.

Define internally normalized effective degrees of freedom relative to one photon-role carrier on the same geometry:

\[
g_{*,\rho}^{D}(T)
=\frac{E_D(T)}{E_{\gamma,1}(T)},
\qquad
 g_{*,s}^{D}(T)
=\frac{S_{\mathrm{th},D}(T)}{S_{\gamma,1}(T)}.
\]

These are generated functions of the Module C spectrum and Module D evolution. No conventional \(g_*\) table is imported.

Recursive memory entropy remains a separate object:

\[
S_{\mathrm{rec},D}
=-\sum_jp_j\log p_j.
\]

It cannot close a thermodynamic heat or energy budget.

---

## 12. Finite-temperature masses, widths, screening, and response

Let \(q\) denote any admitted order parameter or constrained microscopic expectation. The exact constrained thermal free energy is

\[
F_D(q;\beta,\boldsymbol\mu,G)
=-\beta^{-1}
\log\operatorname{Tr}
\left[
P_q
\exp[-\beta(\mathbb H_C-\boldsymbol\mu\cdot\mathbf Q)]
P_q
\right].
\]

The finite-temperature curvature matrix is

\[
\mathcal M_D^2(\beta)
=\left.\nabla_q\nabla_qF_D(q;\beta)\right|_{q=q_*(\beta)}.
\]

Its eigenvalues define the exact finite-branch thermal mass curvatures. Nonzero decay widths and damping rates are the negative real parts of nonstationary Liouvillian eigenvalues in the corresponding response sector.

Charge screening is generated by

\[
\chi_{AB}
=\frac{1}{V_D}
\frac{\partial^2\log Z_D}
{\partial(\beta\mu_A)\partial(\beta\mu_B)}.
\]

The linear transport matrix is

\[
\mathsf L_{AB}
=
\langle J_A,(-\mathcal L_{\mathrm{eq}})^+J_B\rangle_{\mathrm{eq}},
\]

where \((\cdot)^+\) is the inverse on the nonstationary subspace. Conductivity, diffusion, viscosity, heat conduction, and drag are the appropriate current projections of \(\mathsf L\).

Thus medium corrections and transport coefficients descend from the exact parent Hamiltonian and event generator rather than independent empirical formulas.

---

## 13. Early relational background and backreaction

Module D continues the Module B geometry; it does not assume a mature Module I background.

Define

\[
V_D(G)=\sqrt{\det{}^+G},
\qquad
\Theta_D
=\frac{1}{d_{\mathrm{eff}}}
\frac{d\log V_D}{dt_{\mathrm{phys}}}.
\]

The geometry and matter are coupled by

\[
\frac{\delta S_B}{\delta G_D}
+
\operatorname{Tr}
\left(
\rho_D\frac{\partial\mathbb H_C}{\partial G_D}
\right)=0.
\]

The sector-resolved energy ledger obeys

\[
\frac{dE_s}{dt_{\mathrm{phys}}}
=-P_s\frac{dV_D}{dt_{\mathrm{phys}}}
+\sum_{s'}\mathcal Q_{s'\to s},
\]

\[
\sum_s\mathcal Q_{s'\to s}=0
\]

for internal exchange, with geometry work carried by the complementary parent ledger. Therefore the total matter-plus-geometry energy is conserved.

No Friedmann equation or observed expansion history is inserted. Module I must later embed or reproduce this frozen early segment.

---

## 14. Electroweak-facing thermal evolution

Use the Module C RFL stabilization coordinate

\[
q_R=\operatorname{Tr}(\rho_D\varphi_C^\dagger\varphi_C)
\]

and its exact constrained free energy \(F_R(q_R;\beta)\).

The stabilized branch is

\[
q_R^*(\beta)=\arg\min_{q_R}F_R(q_R;\beta).
\]

The phase history is classified without an inserted transition temperature:

- **crossover:** one minimum moves continuously;
- **first-order event:** two minima become equal with a positive barrier and lawful nucleation routes;
- **higher-order event:** the stable curvature vanishes without coexistence;
- **multiple events:** the global minimizer changes more than once;
- **no sharp event:** no branch-changing or curvature-zero surface occurs.

The internally generated event surface is the root of the appropriate free-energy and stability conditions. If multiple minima exist, bubble, wall, defect, and gravitational-wave source branches are admitted only when the spatial Hessian contains an unstable mode and the event-lift witness closes.

All RFL-carrier masses and interaction matrices are reevaluated from the thermal curvature and exact state, not from a target crossover temperature.

---

## 15. Asymmetry transport and washout

For each charge operator \(Q_A\), define

\[
n_A=V_D^{-1}\operatorname{Tr}(Q_A\rho_D).
\]

Its exact evolution is

\[
\dot n_A
=V_D^{-1}
\operatorname{Tr}
\left(Q_A\mathcal L_D[\rho_D]\right)
-d_{\mathrm{eff}}\Theta_Dn_A.
\]

Only Module C-authorized charge-violating routes contribute to the first term. Splitting the route family gives, as exact moments of the master equation,

\[
\dot{\mathbf n}_Q
=\mathbf S_{CP}
+\mathbf S_{\mathrm{top}}
+\mathsf D_Q\nabla^2\mathbf n_Q
-\mathsf W_Q\mathbf n_Q
+\mathbf T_{\mathrm{dark}}
-d_{\mathrm{eff}}\Theta_D\mathbf n_Q.
\]

The source, conversion, diffusion, washout, dark transfer, and dilution operators are projections of \(\mathcal L_D\); they are not independently selected functions.

On any linear charge subspace, the exact solution is

\[
\mathbf n_Q(t)
=U_Q(t,t_0)\mathbf n_Q(t_0)
+
\int_{t_0}^{t}U_Q(t,u)\mathbf S_Q(u)\,du.
\]

The generated baryon-photon state is

\[
\eta_{B,D}(t)
=\frac{n_B(t)-n_{\bar B}(t)}{n_\gamma(t)}.
\]

It may vanish. No branch is selected or retuned because of its final value.

---

## 16. Strong-sector transition and hadronization

Module C supplies the triadic-fiber singlet projector \(P_{\mathbf1}\), the bound-state operator, and the hadron registry.

Define the deconfined and singlet constrained partition functions

\[
Z_{\mathrm{dec}}(\beta)
=\operatorname{Tr}
\left[P_{\mathrm{dec}}e^{-\beta\mathbb H_C}\right],
\]

\[
Z_{\mathrm{sing}}(\beta)
=\operatorname{Tr}
\left[P_{\mathbf1}e^{-\beta\mathbb H_C}\right].
\]

The exact branch free energies are

\[
F_{\mathrm{dec}}=-\beta^{-1}\log Z_{\mathrm{dec}},
\qquad
F_{\mathrm{sing}}=-\beta^{-1}\log Z_{\mathrm{sing}}.
\]

The strong-sector thermal event is determined by the minimizer and event connectivity of these generated functions. Population transfer into hadrons is executed by the legal singlet-forming jump operators. Nonsinglet states remain internal and never enter the asymptotic registry.

At finite relational size the history may be a smooth crossover. A true phase singularity is claimed only if it survives the declared refinement limit.

Baryon/antibaryon survival, resonance decay, entropy release, and energy transfer are direct marginals of the same evolution.

---

## 17. Particle-antiparticle pair balance

For every conjugate pair \((a,\bar a)\), the Module C event grammar provides

\[
a+\bar a\leftrightarrow\mathcal Y_r
\]

for every legal neutral output family \(\mathcal Y_r\).

The pair state evolves through the exact jump operators and inverse routes. For any conserved charge vector \(q\), the stoichiometric identity

\[
q^{\mathsf T}\nu_r=0
\]

holds. Energy and event ancestry move into the output carrier; no state is deleted.

Residual pair populations, chemical potentials, photon and neutrino heating, and asymmetry dilution are generated outputs.

---

## 18. Freeze-out, freeze-in, decoupling, and recoupling

For a process family \(R\), let \(\mathcal L_R\) be the restricted thermal generator and define its relaxation gap

\[
\lambda_R(t)
=\min_{\lambda\ne0}
[-\operatorname{Re}\lambda(\mathcal L_R(t))].
\]

Define the physical evolution rate

\[
\Xi_D(t)
=\max\left(
|\Theta_D|,
\left\|\frac{dP_R}{dt}\right\|,
\frac{\|\dot{\mathcal L}_R\|}
{\max(\|\mathcal L_R\|,t_B^{-1})}
\right).
\]

A freeze or decoupling surface is generated by

\[
\boxed{\lambda_R(t)=\Xi_D(t)}
\]

with branch orientation determined by the crossing derivative.

- coupled: \(\lambda_R>\Xi_D\);
- decoupling/freeze-out: downward crossing;
- recoupling: upward crossing;
- freeze-in: the integrated relaxation depth never reaches unity;
- partial decoupling: only a proper subspace loses its gap.

The event width and covariance follow from the uncertainty in the two sides and the crossing slope. No fixed temperature is inserted.

---

## 19. Neutrino density-matrix transport

For each relational momentum cell \(\kappa\), let \(\rho_{\nu\kappa}\) be the flavor/mass density matrix inherited from Module C.

Its exact equation is the neutrino block of \(\mathcal L_D\):

\[
\dot\rho_{\nu\kappa}
=-i[H_{\nu\kappa}^{C}+H_{\mathrm{med},\kappa}[\rho_D],
\rho_{\nu\kappa}]
+\mathcal C_{\nu\kappa}[\rho_D].
\]

Here

\[
H_{\mathrm{med},\kappa}
=
\frac{\delta}{\delta\rho_{\nu\kappa}}
\operatorname{Tr}(\rho_D\mathbb H_C)
-H_{\nu\kappa}^{C}
\]

and \(\mathcal C_{\nu\kappa}\) is the projection of the complete jump family.

This evolution preserves positivity and trace. It generates flavor conversion, coherence loss, production, absorption, energy exchange, and spectral distortion.

Neutrino decoupling is the loss of the collision/transport gap relative to \(\Xi_D\), evaluated mode by mode. The photon-neutrino temperature relation and any effective radiation contribution are calculated from the generated spectra. A fixed \(N_{\mathrm{eff}}\) is not an input.

---

## 20. Electron-positron annihilation and photon heating

The electron-positron channel family is the exact subset

\[
e^-+e^+\leftrightarrow\gamma_C+\gamma_C
\]

plus every additional Module C-authorized output.

For sector \(s\), define the event-resolved energy current

\[
\mathcal Q_s^{(r)}
=\operatorname{Tr}
\left(H_s\mathcal L_r[\rho_D]\right).
\]

Every internal event satisfies

\[
\sum_s\mathcal Q_s^{(r)}=0.
\]

Photon heating, neutrino leakage, chemical-potential relaxation, spectral distortions, and thermodynamic entropy production are therefore generated by the collision history. The photon-neutrino temperature ratio is an output of the moment-matching projections after the sectors partially decouple.

---

## 21. Visible-dark exchange

Only Module C-authorized dark-visible jump operators are admitted.

For each sector pair \(s,s'\), define

\[
\mathcal Q_{s\to s'}
=\operatorname{Tr}
\left(H_{s'}\mathcal L_{s\to s'}[\rho_D]\right),
\]

and the corresponding entropy and number currents where meaningful.

The compression relic and dissipative tail retain separate states and ledgers. A missing microscopic witness sets the corresponding transfer generator exactly to zero. Dormancy is not a free parameter.

No dark component may absorb a residual that failed to close elsewhere.

---

## 22. Plasma and collective response

The exact susceptibility matrix \(\chi\) and transport matrix \(\mathsf L\) generate:

- screening lengths from charge-response eigenvalues;
- plasma frequencies from conservative response poles;
- conductivity from electric-current response;
- diffusion from number-current response;
- viscosity from stress response;
- heat conduction from energy-current response;
- drag from cross-sector current response.

A hydrodynamic or magnetohydrodynamic closure is admitted only when the nonhydrodynamic Liouvillian gaps remain parametrically faster than the retained modes and the closure error is below the declared tolerance.

This condition is derived from the spectrum rather than assumed from a mean-free-path slogan.

---

## 23. Magnetogenesis, turbulence, shocks, and defects

Linearize the coupled geometry-thermal generator around the physical branch:

\[
\delta\dot X=\mathcal J_D(t)\delta X.
\]

A nonlinear structure is opened by CIF\(_D\) only if an eigenvalue or transient singular value of \(\mathcal J_D\) generates a witnessed unstable mode.

QV\(_D\) then evolves the nonlinear branch, and RFL\(_D\) stabilizes or obstructs the resulting structure.

Without such an instability:

- magnetic fields;
- vorticity;
- turbulence;
- shocks;
- bubbles;
- domain walls;
- strings;
- defects;
- stochastic gravitational-wave sources

remain absent. They are not added as optional phenomenology.

---

## 24. Neutron-proton interconversion

Let \(P_n,P_p\) be the Module C neutron-role and proton-role projectors. Define

\[
n_n=V_D^{-1}\operatorname{Tr}(P_n\rho_D),
\qquad
n_p=V_D^{-1}\operatorname{Tr}(P_p\rho_D).
\]

All Module C-authorized weak routes connecting the two projectors enter the jump family. The exact two-reservoir moment system is

\[
\dot n_n
=-\Gamma_{n\to p}n_n
+\Gamma_{p\to n}n_p
-\Gamma_{n,\mathrm{dec}}n_n
-d_{\mathrm{eff}}\Theta_Dn_n,
\]

\[
\dot n_p
=+\Gamma_{n\to p}n_n
-\Gamma_{p\to n}n_p
+\Gamma_{n,\mathrm{dec}}n_n
-d_{\mathrm{eff}}\Theta_Dn_p,
\]

where every rate is a marginal of the exact neutrino/electron/positron-dependent jump system.

The weak relaxation gap generates the weak freeze-out surface. The neutron-to-proton distribution and covariance are propagated from the full state rather than reduced to a hand-assigned terminal ratio.

---

## 25. Nuclear-readiness hypersurface

Let \(\mathfrak N_E\) be the predeclared Module E legal nuclear-route interface, containing no public rates or abundances.

The Module D→E surface is

\[
\Sigma_{D\to E}
=\inf\left\{t:
\begin{array}{l}
\text{all required D state variables are defined and restartable},\\
\text{probability, energy, charge, entropy-transfer and covariance ledgers close},\\
\text{the nonnuclear fast-mode classification is complete},\\
\text{the kinematic domain of at least one }\mathfrak N_E\text{ route is nonempty}
\end{array}
\right\}.
\]

The exported state is

\[
\boxed{
P_{D\to E}
=(G_D,T_D,A_D,\rho_D^E,P_D,S_{\mathrm{th},D},
F_{\gamma,D},F_{\nu,D},F_{e,D},F_{p,D},F_{n,D},
W_{np,D},\eta_{B,D},\eta_{L,D},\mu_D,
 g_{*,\rho}^{D},g_{*,s}^{D},\mathrm{Plasma}_D,
\mathrm{Dark}_D,\mathrm{Inhom}_D,\Sigma_D,
M_{\mathrm{rec},D},\mathrm{Anc}_D).
}
\]

Module E receives the complete history and restart state. It does not receive a solved nuclear abundance vector.

---

## 26. Uncertainty and branch propagation

Let \(F_D\) denote the complete solution map from the Module C parent to the Module D history. The tangent evolution is

\[
\dot J_{F_D}
=
D\mathcal L_D[\rho_D]\,J_{F_D}
+\partial_{P_C}\mathcal L_D[\rho_D].
\]

The covariance is

\[
\Sigma_D
=J_{F_D}\Sigma_CJ_{F_D}^{\dagger}
+\Sigma_{\mathrm{branch}}
+\Sigma_{\mathrm{thermal}}
+\Sigma_{\mathrm{transport}}
+\Sigma_{\mathrm{phase}}
+\Sigma_{\mathrm{numeric}}
+\Sigma_{\mathrm{trunc}}.
\]

Distinct microscopic, phase, asymmetry, dark-transfer, decoupling, or instability branches remain distinct. No public-facing branch selection is permitted.

Refinement error is bounded by the inherited geometric kernel tail plus the newly opened legal route tail. A material nonconvergent sequence is an obstruction.

---

## 27. Triad-and-kernel necessity

| Removal or corruption | Necessary consequence |
|---|---|
| remove CIF | no complete thermal configuration or channel space |
| remove QV | no collisions, propagation, conversion, transport, phase crossing, or decoupling |
| remove RFL | no stable distributions, equilibrium manifolds, relic survivors, memory, or nuclear-ready state |
| reorder the triad | source, event, and stabilized thermal state become type-inconsistent |
| remove recursive kernel | rates lose depth hierarchy, damping, and inherited modal amplitudes |
| remove directed route pairs | inverse channels, detailed balance, and microscopic reversibility fail |
| remove witnesses | channels can enter without lawful microscopic support |
| remove event lift | collisions, decay, annihilation, phase conversion, and hadronization lose represented boundaries |
| remove N-body completion | constituent relations, collision routing, refinement, and multibody thermal events are incomplete |
| remove no-loss memory | annihilation and phase conversion erase ancestry and carrier content |
| remove branch preservation | distinct transition and decoupling histories are averaged into a false state |
| identify recursive entropy with heat entropy | thermodynamic conservation can be falsely closed |
| replace distributions with markers | no kinetic, transport, entropy-transfer, or nuclear-readiness theorem remains |
| import public targets | the internally generated universe is no longer a prediction |

The full Module D result therefore depends functionally on the enhanced completed Module A triad kernel.

---

## 28. Completion and claim boundary

Module D proves, at its declared scope:

- a complete finite thermal configuration space descended from Module C;
- an exact triad-kernel event generator;
- a unique positive normalized nonequilibrium history;
- channel-complete forward and reverse thermal routing;
- exact distribution marginals and collision operators;
- dynamically earned equilibrium and sector temperatures;
- thermodynamic and recursive-memory entropy separation;
- generated equation-of-state and effective-degree functions;
- exact finite-temperature curvature, damping, screening, and transport operators;
- early relational background and backreaction closure;
- electroweak-facing and strong-sector phase classification and evolution;
- asymmetry transport and washout;
- pair balance and annihilation;
- generated freeze-out, freeze-in, decoupling, and recoupling surfaces;
- neutrino density-matrix transport and spectral evolution;
- electron-positron entropy transfer and photon heating;
- authorized visible-dark exchange or exact dormancy;
- plasma-response and instability criteria;
- neutron-proton and weak-history evolution;
- complete uncertainty, branch, memory, ancestry, and nuclear-ready export.

Module D does not claim:

- measured transition temperatures or durations;
- empirical baryon asymmetry, neutrino radiation density, or photon-neutrino ratio;
- conventional continuum Boltzmann/QFT precision beyond the declared refinement;
- a mature Module I cosmological background;
- primordial nuclear abundances;
- recombination, CMB transfer, structure formation, or empirical agreement.

Within those boundaries, **Module D is complete and frozen**. Module E is the active child.