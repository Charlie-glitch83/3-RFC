# Module F — Post-Nuclear Plasma Persistence and Recombination Readiness

## 1. Scientific boundary

Modules A through E are complete and frozen. Module F consumes only the sealed branch-specific Module E parent

\[
P_{E\to F}(\beta,\gamma,\delta,\epsilon)
=
(G_E,T_E,A_E,\rho_E,P_E,S_{\mathrm{th},E},S_{\mathrm{comp},E},Y_E(t),Y_E^{\mathrm{out}},\Sigma_{Y,E},
\mathrm{Iso}_E,\mathrm{Reac}_E,\mathrm{Decay}_E,\mathrm{Freeze}_E,N_{p,E},N_{e,E},F_{\gamma,E},F_{\nu,E},F_{e,E},
\mathrm{Plasma}_E,Q_E,\mathrm{Dark}_E,\mathrm{Pert}_E,\mathrm{Spatial}_E,\Sigma_E,M_{\mathrm{rec},E},\mathrm{Anc}_E,\mathrm{Restart}_E).
\]

Module F derives the continuous physical interval from the post-nuclear state to the onset of nonequilibrium atomic recombination.

The completed Module F theorem is:

> For every admitted Module E branch, the enhanced completed Module A triad kernel, the inherited microscopic and thermal generators, and the complete isotope-resolved post-nuclear state generate a unique branch-specific post-nuclear plasma history on every declared finite refinement. The history preserves stable isotope identities except through explicit witnessed events; evolves residual reactions and decays, photons, neutrinos, electrons, ions, nuclei, fields, perturbations, and authorized dark sectors; derives plasma response, thermal and momentum exchange, opacity, drag, acoustic, diffusion, damping, and atomic-candidate structures; and produces a restartable recombination-entry state for Module G without importing a standard post-BBN background, public ionization history, recombination coordinate, opacity normalization, or CMB target.

The result is established at a **finite-relational, internal-unit, generated-plasma scope**. It does not claim measured atomic data, public recombination-code equivalence, a unique SI calibration, a visibility function, a last-scattering surface, or empirical agreement.

---

## 2. Enhanced completed Module A kernel

Every construction below uses the complete Module A kernel

\[
\widehat{\mathcal K}_{A}(\tau)
=
\sum_{j=1}^{n_A}
\sum_{[r]\in\mathcal A_A(\tau)}
\delta^{-j}e^{-\alpha j\tau}
W_{j,[r]}(\tau)\Phi_{j,[r]}(\tau),
\]

including:

- ordered CIF/QV/RFL roles;
- the First Action \(QV(CIF)\to RFL\);
- recursive depth and modal structure;
- directed relations and route classes;
- local witnesses and event lift;
- completed-shell and legal-incidence closure;
- no-loss quotienting;
- branch and obstruction laws;
- protected identity, memory, promotion, refinement, and reopening;
- uncertainty and covariance;
- terminal N-body relational and solution-generating completion.

The terminal N-body carrier is active wherever Module F represents interacting particles, nuclei, atomic candidates, fields, transport lanes, collective modes, or spatial refinement. Where no physical witness exists, the carrier remains dormant, information-bearing, unchanged, and nonbackreacting.

---

## 3. Module F triadic specialization

Define

\[
CIF_F
=\Phi_F(P_{E\to F},\widehat{\mathcal K}_{A},M_{\mathrm{rec},E}),
\]

\[
QV_F
=\Psi_F(CIF_F,P_{E\to F},\widehat{\mathcal K}_{A},\mathfrak E_E),
\]

\[
RFL_F
=\Omega_F(QV_F,P_{E\to F},\widehat{\mathcal K}_{A}),
\]

\[
H_F
=\Gamma_F(CIF_F,QV_F,RFL_F,P_{E\to F},\widehat{\mathcal K}_{A}).
\]

Here \(\mathfrak E_E\) is the complete residual reaction, decay, transport, photon, neutrino, charged-particle, plasma, and memory event family inherited from Modules C–E.

The roles are indispensable:

- **CIF\(_F\)** opens every lawful persistent-species, residual-reaction, decay, plasma, field, transport, opacity, perturbation, and atomic-candidate possibility admitted by the sealed parent;
- **QV\(_F\)** executes witnessed propagation, exchange, decay, transport, reduction, refinement, atomic activation, branch selection, and obstruction;
- **RFL\(_F\)** stabilizes persistent composition, plasma state, radiation state, atomic readiness, memory, covariance, and the Module G export.

No conventional plasma or recombination equation becomes an RFC law merely by being relabeled with these symbols.

---

## 4. Finite post-nuclear state space

Choose a declared finite relational refinement \(\mathfrak R_F\) of the inherited geometry, momentum/frequency support, angular support, spatial support, and candidate atomic support.

The Module F Hilbert/configuration space is

\[
\mathcal H_F
=
\mathcal H_{\mathrm{iso}}
\widehat\otimes
\mathcal H_{e^-,e^+}
\widehat\otimes
\mathcal H_{\gamma}
\widehat\otimes
\mathcal H_{\nu}
\widehat\otimes
\mathcal H_{\mathrm{field}}
\widehat\otimes
\mathcal H_{\mathrm{atom\,cand}}
\widehat\otimes
\mathcal H_{\mathrm{record}}
\widehat\otimes
\mathcal H_{\mathrm{memory}}.
\]

A microscopic configuration records

\[
\omega_F
=(n_{a\kappa},\rho_{\nu\kappa},n_{\gamma\kappa\ell m},E,B,G,
\mathcal I,\mathcal A,\mathcal R,M_{\mathrm{rec}},\mathrm{Anc}),
\]

where \(a\) ranges over the complete Module E isotope, ion, lepton, and authorized collective registry, \(\kappa\) labels finite momentum/frequency cells, and \(\mathcal I,\mathcal A,\mathcal R\) record ionic, atomic-candidate, and residual-route state.

The full reduced physical state is a positive density operator

\[
\rho_F(t)\succeq0,
\qquad
\operatorname{Tr}\rho_F(t)=1.
\]

Classical distributions, fluid fields, and background variables are marginals of this state or of a declared no-loss reduction.

---

## 5. Post-nuclear generator

Let \(\mathbb H_F(G_F)\) be the finite many-body Hamiltonian obtained by restricting the frozen Module C microscopic operator to the Module E composition and adding only medium, field, atomic-candidate, and geometry terms generated from the inherited state.

For every witnessed event route \(r:i\to f\), define

\[
\mathcal A_r(t)
=
\sum_j\delta^{-j}e^{-\alpha jt}
W_{j,r}(t)\langle f|\Phi_{j,r}|i\rangle,
\]

\[
J_r
=
\frac{P_fV_rP_i}{\|P_fV_rP_i\|}
\]

when the denominator is nonzero, and

\[
\gamma_r(t)
=
\frac{1}{t_B}
|\mathcal A_r(t)|^2
\Delta_{\sigma_r}(E_f-E_i).
\]

A zero projected amplitude obstructs the route.

Conservative geometry-cell and momentum/frequency-cell transport is represented by completely positive trace-preserving shift generators \(\mathcal T_s[G_F]\). The complete Module F evolution is

\[
\boxed{
\frac{d\rho_F}{dt}
=
\mathcal L_F(t)[\rho_F]
=-i[\mathbb H_F(G_F),\rho_F]
+\sum_s\mathcal T_s[G_F][\rho_F]
+\sum_{r\in\mathcal E_F}
\gamma_r
\left(
J_r\rho_FJ_r^\dagger
-\frac12\{J_r^\dagger J_r,\rho_F\}
\right).
}
\]

The exact finite solution is

\[
\rho_F(t)
=\mathcal T
\exp\left[
\int_{t_E}^{t}\mathcal L_F(u)\,du
\right]\rho_F(t_E).
\]

Piecewise continuous coefficients give a unique solution on every finite declared interval.

---

## 6. Positivity, normalization, and exact ledgers

The generator in Section 5 preserves

\[
\rho_F(t)\succeq0,
\qquad
\operatorname{Tr}\rho_F(t)=1.
\]

For any exact conserved operator \(Q_A\) satisfying

\[
[Q_A,\mathbb H_F]=0,
\qquad
[Q_A,J_r]=0
\]

for every active route,

\[
\frac{d}{dt}\operatorname{Tr}(Q_A\rho_F)=0.
\]

Conditional conservation violations receive contributions only from the explicitly typed violating route family.

The full state closes:

- probability;
- baryon and isotope identity;
- electric charge;
- represented lepton numbers;
- matter-plus-radiation-plus-field-plus-geometry energy;
- total momentum on the declared geometry;
- branch weight;
- event, route, and source ancestry;
- uncertainty and covariance typing.

Reduced dissipation never means fundamental deletion. The omitted record/memory carrier retains event output and reopening data.

---

## 7. Composition persistence theorem

For isotope or stable nuclear carrier \(a\), define

\[
Y_a(t)=\frac{1}{N_B}\operatorname{Tr}(N_a\rho_F(t)).
\]

Its exact evolution is

\[
\boxed{
\dot Y_a
=
\sum_{r\in\mathcal R_{\mathrm{res}}}\nu_{ar}\mathcal F_r
+\sum_{d\in\mathcal D_{\mathrm{late}}}\nu_{ad}\mathcal F_d
+\mathcal F_a^{\mathrm{inj}}
+\mathcal F_a^{\mathrm{transport}}.
}
\]

For a spatially homogeneous closed branch, transport integrates to zero. If every residual, decay, injection, and conversion coefficient touching \(a\) vanishes after \(t_*\), then

\[
\dot Y_a(t)=0
\qquad(t\ge t_*).
\]

Thus stable primordial composition persists exactly unless a witnessed event changes it.

For number density,

\[
\dot n_a+d_{\mathrm{eff}}\Theta_Fn_a
=C_a^{\mathrm{res}}+D_a^{\mathrm{late}}+I_a-L_a.
\]

Every nonzero term must identify its source, destination, conservation partner, uncertainty, branch, and ancestry.

---

## 8. Residual reactions and late decays

Module F inherits the complete `Reac_E`, `Decay_E`, and `Freeze_E` registries.

For route \(r\), define its future materiality bound over the generated Module F interval

\[
\mathfrak M_r(t)
=
\sup_{O\in\mathcal O_F}
\left|
\int_t^{t_{G,\mathrm{in}}}
\operatorname{Tr}
\left(O\,\mathcal L_r[\rho_F(u)]\right)du
\right|,
\]

where \(\mathcal O_F\) is the declared set of composition, charge, heat, spectral, opacity, perturbation, and atomic-readiness observables.

A route is:

- **active** when \(\mathfrak M_r\) exceeds the inherited theory/numerical tail;
- **condition-triggered** when its witness domain has not yet opened;
- **retired** only when an operator-norm bound proves \(\mathfrak M_r\) below the declared tolerance;
- **obstructed** when a required witness, conservation pairing, or output state fails.

Active decay energy and daughter production remain explicit. They may not be hidden inside a background correction.

---

## 9. Generated background and coordinate continuation

Let the inherited relational volume be

\[
V_F(G_F)=\sqrt{\det{}^+G_F}.
\]

Define

\[
\Theta_F
=\frac1{d_{\mathrm{eff}}}
\frac{d\log V_F}{dt},
\qquad
 a_F(t)
=\left[\frac{V_F(t)}{V_F(t_E)}\right]^{1/d_{\mathrm{eff}}}.
\]

A redshift-like internal coordinate may be defined only as

\[
1+z_F(t;t_{\mathrm{ref}})
=\frac{a_F(t_{\mathrm{ref}})}{a_F(t)}.
\]

It is not assigned from public cosmology.

For sector \(s\),

\[
\dot E_s
=-P_s\dot V_F
+\sum_{u}\mathcal Q_{u\to s},
\]

with

\[
\sum_s\sum_u\mathcal Q_{u\to s}=0
\]

for internal exchange. Geometry backreaction closes through

\[
\frac{\delta S_{\mathrm{geom}}}{\delta G_F}
+
\operatorname{Tr}
\left(
\rho_F\frac{\partial\mathbb H_F}{\partial G_F}
\right)=0.
\]

The Module F background is the local physical continuation required for plasma evolution. Module I later owns mature geometry, global distances, and horizon structure.

---

## 10. Kinetic, moment, multifluid, and background representations

For species \(s\) on finite phase-space support, define the kinetic state \(f_s(\kappa,x,t)\). Its moments are

\[
M_{s,m}(x,t)
=
\sum_\kappa w_\kappa\psi_m(\kappa)f_s(\kappa,x,t).
\]

Let \(P_m\) be the moment projection and \(R_m\) the maximum-entropy reconstruction on the finite support subject to the retained moments and exact charges.

A reduction is lawful only if:

\[
P_mR_m=I
\]

on the retained moment domain and

\[
\sup_{O\in\mathcal O_{\mathrm{mat}}}
\left|
\operatorname{Tr}[O(\rho_F-R_mP_m\rho_F)]
\right|
\le\varepsilon_{\mathrm{red}},
\]

with discarded covariance and ancestry recorded.

The allowed hierarchy is

```text
full kinetic
-> moment hierarchy
-> multifluid
-> single fluid
-> background reduction
```

and the reverse promotion when a discarded mode becomes material. A fluid reduction may not erase a material spectral tail, velocity slip, anisotropic stress, isotope distinction, or branch.

---

## 11. Charge closure and relational Gauss law

Let \(B_F\) be the oriented incidence matrix of the active relational geometry and \(C_F\succ0\) the generated edge-capacitance/charge-response matrix. Define

\[
L_Q=B_F^{\mathsf T}C_FB_F.
\]

For local charge vector \(\rho_Q\), global closure is

\[
\mathbf1^{\mathsf T}\rho_Q=0.
\]

On a connected branch, the mean-zero potential is

\[
\phi_Q=L_Q^+\rho_Q,
\]

and satisfies

\[
L_Q\phi_Q=\rho_Q,
\qquad
\mathbf1^{\mathsf T}\phi_Q=0.
\]

The edge field is

\[
E_Q=-B_F\phi_Q.
\]

This distinguishes exact global neutrality, local quasineutrality, finite charge separation, and numerical constraint error. Electron density follows from the generated ionic/atomic state and the charge ledger; it is never supplied as an external ionization fraction.

---

## 12. Plasma response and collective modes

For charge/current observables \(Q_A,J_A\), define the equilibrium or local-stationary susceptibility

\[
\chi_{AB}
=\frac{\beta}{V_F}
\left(
\langle Q_AQ_B\rangle
-\langle Q_A\rangle\langle Q_B\rangle
\right).
\]

The retarded finite response is

\[
\Pi^R_{AB}(\omega)
=\left\langle
J_A,
(i\omega-\mathcal L_F)^{-1}J_B
\right\rangle.
\]

The dielectric/response operator is

\[
\varepsilon_F(\omega,k)
=I-\mathcal V_Q(k)\Pi^R(\omega,k),
\]

and collective modes are generated by

\[
\det\varepsilon_F(\omega,k)=0.
\]

Screening lengths, plasma frequencies, damping, charge-restoration times, and instabilities are eigenproperties of this generated operator. They are not copied constants.

The transport matrix is

\[
\mathsf L_{AB}
=
\left\langle
J_A,(-\mathcal L_{\mathrm{fast}})^+J_B
\right\rangle,
\]

from which conductivity, resistivity, Hall response, diffusion, thermal conduction, viscosity, and drag are obtained by the appropriate current projections.

A hydrodynamic closure is admitted only when every discarded nonhydrodynamic mode relaxes faster than the retained evolution and its reconstruction error is below the no-loss bound.

---

## 13. Photon distribution and spectral persistence

Let \(\rho_{\gamma\kappa\ell m}\) denote the photon density matrix on frequency, angular, and polarization support. Its exact marginal equation is

\[
\boxed{
\dot\rho_\gamma
=\mathcal T_\gamma[G_F]\rho_\gamma
+\mathcal C_{\gamma e}\rho_F
+\mathcal C_{\gamma i}\rho_F
+\mathcal E_{\mathrm{ff}}\rho_F
+\mathcal E_{2\gamma}\rho_F
+\mathcal I_{\mathrm{decay}}\rho_F
+\mathcal I_{\mathrm{dark}}\rho_F.
}
\]

Every nonzero term must descend from an admitted upstream vertex and witness.

The geometry transport \(\mathcal T_\gamma\) is the conservative pushforward between relational frequency cells generated by \(G_F(t)\). It preserves photon-state positivity and total represented phase-space measure while allowing energy change through the paired geometry ledger.

A blackbody or generalized Gibbs reduction is lawful only when the information-projection residual

\[
D(\rho_\gamma\|\rho_{\gamma,\mathrm{G}}^*)
\]

and every material spectral mode lie below the declared bound. Chemical-potential-like, Comptonization, free-free, two-photon, decay, annihilation, and dark-injection distortions remain explicit when material.

---

## 14. Neutrino and decoupled-species persistence

For each neutrino momentum/angular cell,

\[
\dot\rho_{\nu\kappa}
=-i[H_{\nu\kappa}^{C}+H_{\mathrm{med},\kappa},\rho_{\nu\kappa}]
+\mathcal T_\nu[G_F]\rho_{\nu\kappa}
+\mathcal C_{\nu\kappa}^{\mathrm{res}}[\rho_F].
\]

This preserves positivity, flavor coherence, antineutrino distinction, energy/pressure, anisotropic stress, residual interaction history, perturbations, covariance, and ancestry.

A free-streaming reduction is earned when the collision gap remains below the physical transport/evolution rate while the retained angular hierarchy converges. An effective radiation scalar is a derived summary only; it does not replace the underlying state when Module G or H requires that state.

---

## 15. Electron, ion, photon, and sector thermal exchange

For sector \(s\),

\[
\dot E_s
=-P_s\dot V_F
+\sum_{u\ne s}\mathcal Q_{u\to s}
+\mathcal Q_{\mathrm{decay}\to s}
+\mathcal Q_{\mathrm{field}\to s},
\]

where

\[
\mathcal Q_{u\to s}
=\operatorname{Tr}(H_s\mathcal L_{u\to s}[\rho_F]).
\]

For every closed exchange pair,

\[
\mathcal Q_{u\to s}=-\mathcal Q_{s\to u}.
\]

The system includes, where witnessed:

- electron-photon Compton-like exchange;
- electron-ion Coulomb-like exchange;
- ion-photon momentum exchange;
- residual pair processes;
- decay heating;
- conductive and diffusive transport;
- adiabatic geometry work;
- authorized visible-dark exchange.

Sector temperatures exist only through moment-matching maximum-entropy projections. Distinct temperatures remain active until the exchange Liouvillian gaps dominate the background and perturbation rates and the projection residual is below tolerance.

---

## 16. Equation of state and acoustic structure

Define

\[
\rho_F^E=\frac{\operatorname{Tr}(\rho_F\mathbb H_F)}{V_F},
\]

\[
P_F
=-\frac1{d_{\mathrm{eff}}V_F}
\operatorname{Tr}
\left(
\rho_F\frac{\partial\mathbb H_F}{\partial\log G_F}
\right).
\]

The thermodynamic Hessian on the retained state variables \(X\) is

\[
\mathcal H_{\mathrm{th}}
=\nabla_X^2E_F(X).
\]

The multicomponent sound and entropy modes are generalized eigenmodes of the linearized conservative flux Jacobian relative to the susceptibility matrix. Along an adiabatic physical branch,

\[
c_s^2
=\left.\frac{dP_F}{d\rho_F^E}\right|_{S,Q,Y}.
\]

Photon-baryon acoustic modes, slip modes, entropy modes, and damping are derived from the linearized full generator. A familiar sound-speed formula is admitted only when it is the exact reduction of this generated state.

---

## 17. Opacity and scattering seed registry

For photon mode \(\kappa\) and material route \(r\), define the extinction rate

\[
\Gamma_{r,\kappa}^{\mathrm{ext}}
=
\gamma_r
\operatorname{Tr}(J_r^\dagger J_r\rho_F)
\ge0.
\]

The total opacity seed is

\[
\boxed{
\kappa_F(\kappa,t)
=\frac{1}{v_{\gamma\kappa}V_F}
\sum_{r\in\mathcal R_{\gamma,\mathrm{mat}}}
\Gamma_{r,\kappa}^{\mathrm{ext}}.
}
\]

The redistribution kernel is

\[
\mathcal R_F(\kappa\to\kappa',t)
=\sum_r
\gamma_r
\operatorname{Tr}
\left(
P_{\kappa'}J_rP_\kappa\rho_FJ_r^\dagger
\right).
\]

The registry may contain scattering, free-free, two-photon, bound-free precursor, bound-bound precursor, Rayleigh-like, decay, residual-pair, and authorized dark routes only when their inherited vertices and witnesses are nonzero.

Module F constructs opacity **seeds**. Module G later evolves atomic populations and integrates physical optical depth and visibility.

---

## 18. Momentum drag, energy exchange, tight coupling, diffusion, and damping

The photon-baryon momentum exchange is the first momentum moment of the same collision operator:

\[
\mathcal D_{\gamma b}
=\sum_\kappa p_\kappa
\operatorname{Tr}
\left(
N_{\gamma\kappa}\mathcal C_{\gamma b}[\rho_F]
\right).
\]

The paired baryon term is equal and opposite. Energy and momentum exchange are retained as distinct projections.

Let \(\lambda_{\mathrm{drag}}\) be the relaxation gap of the relative-velocity block and \(\Xi_F(k,t)\) the maximum background, propagation, and perturbation rate. Tight coupling is earned when

\[
\lambda_{\mathrm{drag}}\gg\Xi_F
\]

and the Schur-complement reduction error is below tolerance.

For a fast collision block \(\mathcal L_{\mathrm{fast}}\), the generated diffusion matrix is

\[
\mathsf D_F
=-\mathsf J^{\mathsf T}
\mathcal L_{\mathrm{fast}}^+
\mathsf J,
\]

which is positive semidefinite on the physical current subspace. Damping rates are the negative real parts of the corresponding slow eigenvalues after the fast block is eliminated.

---

## 19. Perturbation and spatial continuity

Let \(X_F\) contain all active background, kinetic, fluid, field, composition, and atomic-candidate variables. Perturbations obey

\[
\delta\dot X_F
=\mathcal J_F(t,k)\delta X_F,
\qquad
\mathcal J_F=D_X\mathcal F_F.
\]

This carries:

- density and velocity perturbations;
- photon-baryon slip;
- pressure and entropy perturbations;
- composition and charge perturbations;
- anisotropic stress;
- photon and neutrino angular moments;
- electromagnetic and dark perturbations;
- inherited magnetic, vortical, turbulent, shock, defect, or spatial branches where active;
- cross-sector covariance and phase.

A background-only reduction is permitted only after an operator bound proves every omitted perturbative effect below the declared Module F and downstream tolerances.

Module H later owns the universal linear-response hierarchy. Module F preserves and conditions its physical parent.

---

## 20. Fields, instabilities, and dark-sector continuity

Linear and nonlinear field structures are not generic add-ons. They exist only when inherited from Module E or generated by an instability of the coupled Module F Jacobian.

For a candidate mode,

\[
\delta\dot X=\mathcal J_F\delta X.
\]

An instability branch opens only when a witnessed eigenvalue or transient singular-value growth exceeds damping and truncation uncertainty.

Compression-relic and dissipative-tail states retain separate ledgers. An authorized dark-visible route contributes through the same amplitude, event, conservation, energy, momentum, and ancestry laws as every visible route. If no witness exists,

\[
\mathcal L_{\mathrm{dark}\leftrightarrow\mathrm{vis}}=0.
\]

No dark term may absorb a failed residual or create a desired atomic-readiness history.

---

## 21. Atomic-candidate Hamiltonian

Module F opens atomic carriers from the inherited nuclei, electrons, photons, charge law, and relational geometry.

For nucleus/isotope \(A\), charge state \(q\), and admitted electron number \(N_e\), let \(P_{A,q,N_e}\) be the exact constituent, charge, statistics, and branch projector.

Let \(G_Q^+\) be the mean-zero relational charge Green operator generated by Section 11. The finite atomic-candidate Hamiltonian is

\[
\boxed{
\mathbb H_{A,q}^{\mathrm{atom}}
=P_{A,q,N_e}
\left[
H_A^{E}
+\sum_{i=1}^{N_e}h_{e_i}^{C}
+\frac12\mathbf Q^{\mathsf T}G_Q^+\mathbf Q
+\mathcal K_{\mathrm{spin}}
+\mathcal K_{\mathrm{rad}}
+\Sigma_{\mathrm{plasma}}
\right]
P_{A,q,N_e}.
}
\]

Every term descends from Module C microscopic vertices, Module E nuclear identity, the enhanced kernel, or the generated Module F medium. No measured level or ionization energy is inserted.

Let

\[
\mathbb H_{A,q}^{\mathrm{atom}}|A,q,\lambda\rangle
=E_{A,q,\lambda}|A,q,\lambda\rangle.
\]

A bound atomic candidate is admitted only when

\[
B_{A,q,\lambda}^{\mathrm{atom}}
=E_{\mathrm{ion\ threshold}}-E_{A,q,\lambda}>0
\]

and its witness, statistics, charge, transition, memory, and no-loss conditions close.

---

## 22. Atomic, ionic, continuum, and transition seed registry

The atomic candidate registry contains, where generated and material:

```text
isotope and nuclear parent,
ionic charge state,
constituent electron number,
level/superlevel identity,
energy and binding,
degeneracy and angular representation,
metastability class,
continuum threshold,
plasma shift and covariance,
branch, memory, and ancestry
```

For inherited electromagnetic/photon vertex \(V_\gamma\), transition seeds are generated by

\[
\mathcal M_{ab}^{(\gamma)}
=\langle b|V_\gamma|a\rangle.
\]

A bound-bound, bound-free, free-bound, two-photon, collisional, excitation, de-excitation, ionization, or capture route enters only when the projected matrix element, conservation witness, kinematic domain, inverse-route status, and no-loss event lift close.

Module F need not evolve the full multilevel population history. It must provide the complete candidate and seed system required for Module G at the declared precision.

---

## 23. Atomic promotion and ionization reopening

Atomic formation is a represented state transformation, not deletion of nuclei and electrons.

Let

\[
U_{\mathrm{atom}}:
\mathcal H_A\otimes\mathcal H_{e}^{\otimes N_e}\otimes\mathcal H_{\gamma,\mathrm{record}}
\to
\mathcal H_{A,q}^{\mathrm{atom}}\otimes\mathcal H_{\mathrm{record}}
\]

be the event-lift isometry generated by the admitted capture route. It satisfies

\[
U_{\mathrm{atom}}^\dagger U_{\mathrm{atom}}=I
\]

on the admitted input subspace and preserves exact charge, energy with the emitted/absorbed photon carrier, angular momentum, constituent identity, memory, and ancestry.

Ionization/refinement uses the lawful inverse or conjugate route. The atomic state can therefore reopen to its nuclear, electron, photon, route, and event parents without physical backward time evolution.

---

## 24. Atomic readiness criterion

Let \(P_{\mathrm{atom}}\) project onto all admitted atomic candidates and \(P_{\mathrm{free}}=I-P_{\mathrm{atom}}\). Define

\[
J_{\mathrm{cap}}(t)
=\operatorname{Tr}
\left[
P_{\mathrm{atom}}\mathcal L_{\mathrm{free}\to\mathrm{atom}}[\rho_F]
\right],
\]

\[
J_{\mathrm{ion}}(t)
=\operatorname{Tr}
\left[
P_{\mathrm{free}}\mathcal L_{\mathrm{atom}\to\mathrm{free}}[\rho_F]
\right].
\]

Let \(\lambda_{\mathrm{atom}}(t)\) be the smallest nonzero relaxation/destruction gap of the atomic block, \(\Xi_F(t)\) the maximum background/transport variation rate, and \(\epsilon_{K,F}\) the inherited kernel-plus-refinement tail.

Define the slow persistent atomic projection

\[
p_{\mathrm{atom}}^{\mathrm{slow}}
=\operatorname{Tr}
\left(P_{\mathrm{slow,atom}}\rho_F\right).
\]

Atomic kinetics become a distinct required child problem when:

1. the atomic-candidate registry is nonempty;
2. the transition seed graph is complete over the declared domain;
3. \(p_{\mathrm{atom}}^{\mathrm{slow}}>\epsilon_{K,F}\);
4. the net integrated capture into the slow subspace is positive over the local RFL stabilization interval;
5. the atomic block is no longer safely eliminable, expressed by \(\lambda_{\mathrm{atom}}\lesssim\Xi_F\) or an equivalent failed reduction bound;
6. the opacity/electron-density sensitivity to atomic populations exceeds the same inherited tail.

This criterion detects when atomic competition becomes physically material without assigning a public recombination temperature or redshift.

---

## 25. Recombination-entry surface

The generated Module G entry time is

\[
\boxed{
t_{G,\mathrm{in}}
=\inf\left\{t:
\begin{array}{l}
\text{post-nuclear composition is persistent},\\
\text{every material residual reaction/decay is active, scheduled, or bounded},\\
\text{photon, neutrino, electron, ion, plasma, field, and perturbation states are restartable},\\
\text{charge, energy, momentum, entropy-transfer, and covariance ledgers close},\\
\text{atomic readiness conditions 1--6 hold},\\
\text{Module G requires no imported physical parameter}
\end{array}
\right\}.
}
\]

The surface is

\[
\Sigma_{G,\mathrm{in}}
=\left\{
 t,\eta,a_F,z_F,\Theta_F,\rho,P,
 T_m,T_e,T_i,T_\gamma,T_\nu,
 f_\gamma,f_\nu,Y_{\mathrm{iso}},n_b,n_e,
 \mu,\mathrm{Plasma},\kappa_F,\mathrm{Drag},
 \mathrm{Sound},\mathrm{Diffusion},\mathrm{Damping},
 \mathrm{Atomic},\mathrm{Transition},\mathrm{Pert},\mathrm{Dark},
 \Sigma,M_{\mathrm{rec}},\mathrm{Anc}
\right\}_{t=t_{G,\mathrm{in}}}.
\]

Atomic readiness is not solved recombination. Module G owns the nonequilibrium population, opacity, optical-depth, visibility, last-scattering, and drag histories.

---

## 26. Entropy and memory typing

Module F preserves distinct ledgers:

\[
S_{\mathrm{th},F}=-\operatorname{Tr}(\rho_F\log\rho_F),
\]

\[
S_{\gamma,F}=-\operatorname{Tr}(\rho_\gamma\log\rho_\gamma),
\qquad
S_{\nu,F}=-\operatorname{Tr}(\rho_\nu\log\rho_\nu),
\]

\[
S_{\mathrm{comp},F}=-\sum_ax_a\log x_a,
\qquad x_a=A_aY_a,
\]

\[
S_{\mathrm{rec},F}=-\sum_jp_j\log p_j.
\]

Thermodynamic and radiative entropy track physical transfer and irreversibility. Compositional entropy tracks the persistent species distribution. Recursive entropy tracks protected depth and ancestry.

Representation reduction must record discarded information and prove its immateriality for Module F and Module G. Two states with identical composition but different spectra, decay histories, perturbations, atomic candidates, branches, or memory remain distinct unless a no-loss equivalence theorem is proved.

---

## 27. Branches, obstruction, and convergence

For a fixed admitted Module E parent, Module F may yield:

- one unique physical history;
- a representation-equivalent family;
- multiple physically distinct histories;
- obstruction.

Distinct residual-decay, spectral, thermal-coupling, plasma-instability, field, dark-sector, spatial, perturbative, or atomic-readiness branches remain distinct.

Refinement proceeds through Module A promotion and add-one laws. Let \(X_F^{(N)}\) be the solution at refinement \(N\). The branch closes over the declared domain only when

\[
\sup_t\|\mathcal O(X_F^{(N+1)})-\mathcal O(X_F^{(N)})\|
\le\varepsilon_{\mathrm{ref}}
\]

for every claimed observable, and conservation, covariance, opacity, transport, atomic-registry, and child-state completeness remain stable.

Negative distributions, charge failure, energy/momentum nonreciprocity, missing material transport, inconsistent reduction overlap, nonconvergent atomic support, or incomplete Module G state is an obstruction. It is not permission to retune Module E.

---

## 28. Uncertainty and covariance propagation

Let \(F_F\) denote the complete Module F solution map. The tangent map obeys

\[
\dot J_F
=D_X\mathcal F_F\,J_F
+D_{P_E}\mathcal F_F.
\]

The covariance is

\[
\boxed{
\Sigma_F
=J_F\Sigma_EJ_F^{\mathsf T}
+\Sigma_{\mathrm{residual}}
+\Sigma_{\mathrm{transport}}
+\Sigma_{\mathrm{radiation}}
+\Sigma_{\mathrm{plasma}}
+\Sigma_{\mathrm{atomic\ seed}}
+\Sigma_{\mathrm{representation}}
+\Sigma_{\mathrm{spatial}}
+\Sigma_{\mathrm{numeric}}
+\Sigma_{\mathrm{branch}}.
}
\]

For linearized stochastic or unresolved finite modes, covariance evolves by

\[
\dot\Sigma
=\mathcal J_F\Sigma+\Sigma\mathcal J_F^{\mathsf T}+\mathcal Q_F,
\qquad
\mathcal Q_F\succeq0.
\]

Uncertainty propagation is not parameter fitting and cannot select a public-facing branch.

---

## 29. Complete Module F → G state

For admitted branch \(\phi\), Module F exports

\[
\boxed{
P_{F\to G}(\beta,\gamma,\delta,\epsilon,\phi)
=(G_F,\mathrm{Coord}_F,\mathrm{Bkg}_F,\mathrm{Comp}_F,\mathrm{IonSeed}_F,
\mathrm{Electron}_F,\mathrm{Positron}_F,\mathrm{Photon}_F,\mathrm{Neutrino}_F,
\mathrm{Temp}_F,\mathrm{Chem}_F,\mathrm{Plasma}_F,\mathrm{OpacitySeed}_F,
\mathrm{DragSeed}_F,\mathrm{Sound}_F,\mathrm{Diffusion}_F,\mathrm{Damping}_F,
\mathrm{Atomic}_F,\mathrm{Transition}_F,\mathrm{Residual}_F,\mathrm{Pert}_F,
\mathrm{Field}_F,\mathrm{Dark}_F,\mathrm{Entropy}_F,\Sigma_F,
\mathrm{Memory}_F,\mathrm{Ancestry}_F,\mathrm{Restart}_F).
}
\]

This state contains:

- physical-time, conformal-time, scale, and internal redshift-like coordinates;
- the generated local background history;
- complete isotope-resolved persistent composition and covariance;
- ionic seeds, electrons, positrons, photons, neutrinos, and sector temperatures;
- chemical potentials and exact charge state;
- plasma, electromagnetic, transport, and collective-mode operators;
- opacity, redistribution, drag, sound, diffusion, and damping seeds;
- complete atomic-candidate, level, continuum, and transition seed registries;
- atomic promotion and ionization reopening maps;
- residual reaction and late-decay instructions;
- perturbation, field, spatial, and authorized dark state;
- all entropy, memory, branch, uncertainty, and ancestry ledgers;
- the exact recombination-entry surface and restart state.

Module G may not reconstruct an omitted post-nuclear history or import a conventional recombination initial condition.

---

## 30. Triad-and-kernel necessity

| Removal or corruption | Necessary consequence |
|---|---|
| remove CIF | no complete persistent-species, plasma, opacity, perturbation, or atomic-candidate possibility space |
| remove QV | no residual decay, transport, exchange, field response, atomic activation, or branch resolution |
| remove RFL | no stable composition, plasma regime, atomic readiness, memory, or Module G export |
| reorder the triad | possibility, physical action, and stabilized post-nuclear state become type-inconsistent |
| remove recursive kernel | rates, hierarchy, refinement, damping, and modal ancestry lose their generator |
| remove N-body completion | multicomponent plasma, atomic constituents, collective modes, transport lanes, and spatial refinement are incomplete |
| remove witnesses | reactions, transport, opacity, or atomic states can enter without lawful physical support |
| remove inverse routes | thermal, scattering, capture/ionization, and detailed-balance closure fail |
| remove event lift | decays, scattering, capture, and field transfer become unrepresented label changes |
| remove no-loss memory | isotope, particle, photon, and atomic ancestry are erased |
| collapse distributions to temperatures | spectral distortion, anisotropic stress, opacity, and nonequilibrium readiness are lost |
| collapse composition to H/He summaries | isotope-sensitive masses, charge, decays, atomic states, and covariance are lost |
| import public histories | the Module F universe ceases to be an internally generated RFC prediction |

Module F therefore depends functionally on the enhanced completed Module A triad kernel and the sealed Module E parent.

---

## 31. Completion and claim boundary

Module F establishes at its declared scope:

- immutable consumption of the complete Module E parent;
- explicit CIF/QV/RFL specialization of the enhanced Module A kernel;
- a finite post-nuclear state space and exact positive normalized evolution;
- exact composition persistence except through witnessed events;
- residual-reaction and late-decay continuation;
- generated background and coordinate continuation;
- lawful kinetic, moment, multifluid, fluid, and background reductions;
- relational Gauss-law charge closure;
- generated plasma response, collective modes, screening, conductivity, diffusion, viscosity, conduction, and drag;
- photon spectral and neutrino/flavor persistence;
- reciprocal electron-ion-radiation-field energy and momentum exchange;
- generated equation-of-state and acoustic structure;
- state-derived opacity, redistribution, drag, tight-coupling, diffusion, and damping seeds;
- perturbation, field, spatial, and dark-sector continuity;
- generated atomic-candidate Hamiltonians, bound-state registry, transition seeds, and promotion/reopening maps;
- a physical atomic-readiness criterion and complete recombination-entry surface;
- entropy/memory separation, branch handling, convergence, covariance, and restart closure;
- complete `P_F->G`.

Module F does not claim:

- measured plasma or atomic coefficients;
- public recombination-code agreement;
- a public recombination temperature or redshift;
- a complete atomic population history;
- integrated optical depth or visibility;
- photon last scattering or baryon-drag release;
- the physical CMB surface;
- mature Module I geometry or Module H transfer functions;
- empirical agreement.

Within those boundaries, **Module F is complete and frozen**. Module G is the active child.